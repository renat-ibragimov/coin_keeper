# Copy to .env and fill in. Placeholders only — never commit real values.
# The full reference is docs/10-infra.md.

# --- database, cache ---
DATABASE_URL=postgresql+asyncpg://coinkeeper:<password>@postgres:5432/coinkeeper
REDIS_URL=redis://redis:6379/0

# Used by docker compose to initialise the postgres container.
POSTGRES_USER=coinkeeper
POSTGRES_PASSWORD=<password>
POSTGRES_DB=coinkeeper

# --- object storage ---
S3_ENDPOINT=http://minio:9000
# Set only on the server, where the browser cannot resolve S3_ENDPOINT: signs
# presigned GET URLs against this host instead (docs/06-media-storage.md).
# Leave empty locally — S3_ENDPOINT already resolves from the host.
S3_PUBLIC_ENDPOINT=https://<domain>/media
S3_BUCKET=coinkeeper-media
S3_ACCESS_KEY=<access-key>
S3_SECRET_KEY=<secret-key>

# --- application ---
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=<random-secret>
DOMAIN=<domain>
PUBLIC_BASE_URL=https://<domain>
CORS_ORIGINS=https://<domain>
# Emergency switch only; open registration is the normal state (docs/07-auth.md).
ALLOW_REGISTRATION=true
# Set to false only when serving over plain http locally.
COOKIE_SECURE=true
LOG_LEVEL=INFO

# --- mail ---
# console = write the message to the log (local development, tests)
# smtp    = really send (server)
MAIL_BACKEND=console
SMTP_HOST=<smtp-host>
SMTP_PORT=587
SMTP_USER=<smtp-user>
SMTP_PASSWORD=<smtp-password>
SMTP_FROM=CoinKeeper <noreply@<domain>>
SMTP_STARTTLS=true

# --- external sources, used from stage 5 onwards ---
NBU_API_BASE=https://bank.gov.ua/NBUStatService/v1/statdirectory
NBU_CATALOG_BASE=https://bank.gov.ua/ua/numismatic-products
UACOINS_BASE=https://www.ua-coins.info
