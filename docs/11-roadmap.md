# 11. Порядок работ

Этапы упорядочены так, чтобы каждый заканчивался **проверяемым результатом**, а не
«написали слой». Следующий этап не начинается, пока не выполнен критерий приёмки предыдущего.

**Текущее состояние (2026-09-14): этапы 0–4 завершены (последняя добавка — часть 7
этапа 4, единая страница «Додати»), этап 4.5 «Украинский конвейер»
идёт (сбор серий, суточный крон цен по собранным сериям работает на сервере). API и
статика фронтенда выкатываются на `coins.renat-ibragimov.com` пушем в `main`. Следующий
блок — этап 4.6 «Админка» (решение владельца 2026-09-10: крон уже работает и невидим,
правка каталога идёт SQL-ом на боевой машине). Веха «Демо» — после него.**

**Дополнение 2026-09-17:** в коде реализованы гостевой доступ к общему каталогу и
карточкам, защищённый доступ к ценам и личным данным, гостевой просмотр раздела
«Моя колекція», мягкие лимиты публичных запросов через Redis и подготовка статических
HTML-страниц каталога для индексации. Центральный Caddy подготовлен к их раздаче.
Гостевой интерфейс использует пустые состояния коллекции и общий диалог входа/регистрации;
после входа пользователь остаётся на исходной странице.
Google OAuth запускается в отдельном окне, чтобы возврат не оставлял страницы
авторизации в истории исходной вкладки.

**Дополнение 2026-09-24:** доведён публичный лендинг Bakost: пояснения интерактивности
перенесены к контролам демо, уточнены сортировка по затратам и счётчик от исходных
трёх позиций, увеличены служебные подписи и контраст тёмной темы. Надпись тёмного
логотипа осветлена генератором с сохранением золотого щита и монеты. Продуктовые
экраны, API и семантика фильтров расходов не менялись.

Правило ведения: каждая завершённая часть этапа обновляет этот документ и затронутые
доки тем же набором коммитов (`CLAUDE.md`, «Правила работы с кодом»).

---

## Этап 0. Документация и артефакты ✅

- [x] Извлечь уцелевшее из повреждённого архива десктопной версии
- [x] Восстановить схему базы (9 миграций) из рабочего снимка
- [x] Восстановить карту интерфейса и тексты из собранного бандла
- [x] Описать бизнес-правила по эталонному коду
- [x] Зафиксировать стек и архитектурные решения

Результат: `docs/`, `legacy/`, `CLAUDE.md`.

---

## Этап 1. Каркас бэкенда и рабочий контур выкладки ✅

Завершён 2026-09-02: `1a2a77c` … `4ee1bc9` (скелет, схема и миграция, auth с подтверждением
email и восстановлением пароля, health, Docker/Caddy, reusable workflow, тесты).

- Структура проекта FastAPI, конфигурация через переменные окружения
- `docker-compose.yml`: postgres, redis, minio, api
- SQLAlchemy-модели по `02-data-model.md`
- Первая миграция Alembic — вся схема целиком
- Аутентификация: регистрация, вход, refresh (cookie), выход, `/auth/me` (`07-auth.md`)
- **Подтверждение email**: `auth_tokens`, `/auth/verify-email`, `/auth/resend-verification`,
  неактивный аккаунт до перехода по ссылке
- **Восстановление пароля**: `/auth/forgot-password`, `/auth/reset-password`
- **Отправка почты**: `MAIL_BACKEND` (`console` локально и в тестах, `smtp` на сервере),
  внешний SMTP (Resend или Postmark), шаблоны двух писем (`10-infra.md`)
- **Ограничение частоты** на Redis по таблице лимитов из `07-auth.md` + honeypot-поле
  в регистрации
- `GET /health`
- pytest + фикстуры с тестовой базой
- **Сервер Hetzner, домен и Caddy подняты**: HTTPS, сертификат, reverse proxy
  (`10-infra.md`)
- **CI/CD работает**: reusable workflow build → lint → test → образы → GHCR → deploy по SSH;
  пуш в `main` выкатывается на домен автоматически
- **Боевой SMTP на сервере**: SPF, DKIM, DMARC настроены вместе с доменом

**Критерий приёмки:** `docker compose up` поднимает окружение, `alembic upgrade head`
создаёт схему; новый пользователь регистрируется сам, получает письмо, подтверждает адрес и
получает токен; до подтверждения войти нельзя; восстановление пароля проходит целиком;
превышение лимита попыток входа отбивается `429`; тесты проходят.

**Пуш в `main` автоматически разворачивается на сервер, `/health` отвечает по HTTPS на
домене.** Регистрация второго администратора проходит с настоящим письмом через боевой SMTP.
Чек-лист первого деплоя из `10-infra.md` пройден целиком.

Этап заканчивается работающим контуром: код из `main` живёт на домене, а не на ноутбуке.
Дальше каждый следующий этап выкатывается тем же способом, и «у меня локально работает»
перестаёт быть валютой.

Обязательный тест на каскады: **удаление пользователя, имеющего личные позиции каталога и
экземпляры на них, проходит одним стейтментом и не оставляет осиротевших строк.** Это проверка
каскадного ромба `users → collection_items` и `users → catalog_items` — заодно с тем, что
общий каталог при удалении пользователя не страдает. Второй тест рядом проверяет, что
внешний ключ по-прежнему не даёт оставить экземпляр без позиции каталога
(`02-data-model.md`, «Почему `NO ACTION`, а не `RESTRICT`»).

---

## Этап 2. Перенос данных ✅

Завершён 2026-09-02: `85bc455` … `690d2b1` (скрипт миграции, обработка изображений,
пометка подозрительных цен, runbook в `09-data-migration.md`).

- Скрипт `migrate_legacy.py` по `09-data-migration.md`
- Владелец создаётся подтверждённым, с ролью `admin`, пароль проходит общую валидацию
- Скрипт `promote_admin.py` — поднятие роли уже зарегистрированного пользователя
  до `admin` (`09-data-migration.md`)
- Каталог переносится как **общий** (`created_by = NULL`) — это затравка общего каталога
- Загрузка 839 уцелевших изображений в MinIO с превью
- Разделение `storage_key` / `external_url` и простановка `media_files.source`
  (`ucoin` / `user_upload`)
- Валидация цен, пометка `is_suspect`
- Отчёт о миграции

**Критерий приёмки:** все проверки из чек-листа `09-data-migration.md` пройдены,
сумма расходов совпадает до копейки — 42 765,66 грн, `source` заполнен у всех записей
`media_files`; владелец входит своим паролем и видит роль `admin`; второй администратор,
зарегистрированный штатно в этапе 1, поднят до `admin` скриптом.

---

## Этап 3. API каталога и коллекции ✅

Завершён 2026-09-02: `7979254` … `cff833d`, доки `0726287` (справочники, чтение каталога,
CRUD и архивация, коллекция с транзакцией покупки, расходы, серии, bootstrap).

- `GET /catalog` со всеми фильтрами, сортировкой и постраничной выдачей
- `GET /catalog/{id}` — карточка
- **Фильтр видимости каталога в репозиторийном слое** (`created_by IS NULL OR created_by =
  :user_id`), рядом с фильтром `owner_id`
- CRUD **личных** позиций каталога; общий каталог только на чтение, правка — по роли admin
  (`07-auth.md`)
- Архивация общего каталога: `is_archived`, `POST /catalog/{id}/archive` и `/unarchive`,
  параметр `archived` в выдаче, исключение архивных из витрины, поиска и комплектности
  (`04-business-rules.md`, п. 10)
- CRUD коллекции с автосозданием расхода и подстановкой курса НБУ; удаление экземпляра
  удаляет расход `coin_purchase` в сервисном слое
- Расходы: `GET/POST/PATCH/DELETE /expenses` и `GET /expenses/summary` (`03-api-contract.md`) —
  без них не собрать экран «Деньги» и не проверить сопутствующие траты. Ручной CRUD
  не касается `coin_purchase`: эти записи живут только вместе с экземплярами коллекции
- Сортировки-агрегаты `owned | purchase | price` через `LATERAL`, а не постобработкой
- `GET /series`, `GET /series/{id}/summary`
- `GET /bootstrap` со всеми показателями дашборда
- Полнотекстовый поиск с учётом `title_uk`

**Критерий приёмки:** через API видны 3063 общие позиции с фильтрами, 620 экземпляров
владельца, дашборд отдаёт корректные суммы; личная позиция второго пользователя не видна
первому, попытка изменить общую запись даёт `403`; удаление экземпляра уменьшает сумму
расходов; архивация позиции убирает её из витрины и из знаменателя комплектности, но
экземпляр владельца, его расход и история цен остаются на месте, а процент комплектности
не превышает 100; тесты на расчёт комплектности, финансов и изоляцию данных проходят.

---

## Этап 4. Фронтенд, первая версия ✅

Делается частями; каждая часть — рабочее приложение, а не слой.

**Часть 1 — каркас, вход, каталог ✅** (2026-09-03, `25d9317` … `b69c5b1`)

- [x] Vite + React 19 + TypeScript + React Router + TanStack Query, маршрутизация;
      заглушки-роуты под следующие части
- [x] Дизайн-система на токенах, две темы (светлая — канон, тёмная — равноправная),
      переключатель в шапке; шрифты Playfair Display + Source Sans 3 самохостингом,
      украинские глифы `є ї ґ` проверены
- [x] Базовый ui-kit: Button, Input, Select, Card, Badge, Tabs, Toggle, Pagination,
      Spinner, EmptyState, ErrorState, Skeleton
- [x] Локализация uk/en, дефолт uk, переключение на лету; все строки — в файлах локализации
- [x] API-клиент: access-токен в памяти, refresh через httpOnly cookie, повтор после 401,
      RFC 7807 → типизированная ошибка; типы из `openapi.json` (`npm run gen:api`)
- [x] Вход, регистрация с honeypot, «проверьте почту», подтверждение адреса, восстановление
      пароля (`08-ui-map.md`)
- [x] Каталог: карточки и таблица, фильтры и сортировки ровно по `GET /catalog`, поиск,
      серверная пагинация, состояние в URL, пометки «моя позиция» / «архивная» /
      «в коллекции», фильтр обсяга (общий / мои); карта полноты — заглушка
- [x] Пустой результат ведёт к созданию личной позиции или импорту, а не в тупик
      (кнопки пока на заглушки)
- [x] Мобильная вёрстка: контекстная нижняя навигация (2 пункта вне «Моєї колекції», 5 —
      внутри, без «Ще»: Налаштування и Адміністрування переехали в меню акаунта,
      2026-09-09), шторка фильтров с черновиком и подтверждением (`08-ui-map.md`)
- [x] Единый `CoinImage`: отсутствующее и недоступное фото выглядят одинаково —
      плейсхолдер-монета, без битых `<img>`
- [x] Статика отдаётся центральным Caddy с SPA-fallback; выкладка пока руками
      (`10-infra.md`, «Фронтенд»)

**Часть 2 — дашборд и карточка монеты ✅** (2026-09-03)

- [x] Дашборд «Огляд» на `/` из одного `GET /bootstrap`: показатели (монет, не вистачає,
      комплектность, стран), «Найближчі до завершення» с прогресс-кольцами,
      разбивка по странам, финансы с разницей «оценка − потрачено», курсы НБУ,
      пустое состояние для нового аккаунта, скелетоны и ошибка с повтором
- [x] Карточка `/catalog/:id`: заголовок по правилу `title_{локаль} → titleOriginal`
      (общий хелпер `coinTitle`, используется и в каталоге), аверс/реверс
      с лайтбоксом, только заполненные характеристики, плашка архивной позиции с
      причиной, «У моїй колекції», «Куплено за / Поточна оцінка», действия,
      инфобокс о видимости цен, «Мої екземпляри та покупки» с курсом «X ₴ за 1 $»,
      404 для чужой личной позиции, возврат к каталогу с сохранением фильтров
- [x] История цен — интерактивный график на `lightweight-charts` (TradingView): зум
      колесом мыши, перетаскивание, быстрый выбор диапазона («1М/6М/1Р/Усі»);
      подозрительные снимки не входят в автомасштаб и не соединяются линией, свои
      снимки — отдельный маркер (2026-09-13, замена прежнего самописного SVG
      без библиотек)
- [x] Тесты: хелпер заголовка, форматирование, дельты дашборда, пустое состояние,
      карточка с/без экземпляров, архив, 404, график
- Расхождения с моками зафиксированы в `08-ui-map.md` («Как реализовано»)

**Часть 3 — коллекция, покупки, серии, «Не вистачає», «Гроші», настройки, автодеплой ✅**
(2026-09-03)

- [x] Коллекция `/collection`: плитки (монет, витрачено, оцінка, серій зібрано), список
      экземпляров из `GET /collection` с поиском, фильтрами по стране и серии,
      сортировкой, видом «картки / список», пагинацией; правка и удаление с явным
      «разом із витратою на покупку»
- [x] Покупка `/collection/new` и `/collection/:id/edit`: выбор позиции инлайн-поиском
      или из карточки, кількість, дата, ціна за шт., валюта из `/currencies`, продавець,
      стан по умолчанию из настроек, нотатка; 422 «немає курсу на дату» — у поля даты
- [x] Личная позиция `/catalog/new`: форма по моку `add_*`, `POST /catalog`, у admin —
      переключатель «до загального каталогу»; после создания — карточка с бейджем
- [x] Серии `/series` и `/series/:id`: прогресс по каждой серии, сортировка по
      комплектности и названию, позиции серии с «Додати покупку» на недостающих
- [x] «Не вистачає» `/missing`: `GET /catalog?owned=false` с фильтрами каталога и сводкой
- [x] «Гроші» `/expenses`: сводка по категориям, список, CRUD сопутствующих расходов;
      `coin_purchase` только для чтения. Графики «Витрати за період» и «За категоріями»
      интерактивны и привязаны к диапазону дат, который выбирает пользователь (плашки
      «1М/3М/6М/1Р» плюс произвольные даты) через `GET /expenses/chart-summary`
      (2026-09-13) — суточная группировка при диапазоне до 31 дня, помесячная дальше;
      неизменные `GET /expenses/summary` остаются источником для плиток KPI и чипов
      фильтра таблицы
- [x] Налаштування `/settings`: ім'я і мова через `PATCH /auth/me`, смена пароля, выход.
      Визуально причёсано и перегруппировано (2026-09-13, доработано в тот же день):
      центрированный заголовок, «Профіль» (+ пароль), «Вигляд», «Каталог і колекція» —
      отдельная карточка «Акаунт» упразднена, роль сидит таблеткой прямо в заголовке
      «Профіль», «Вийти» переехала туда же (по центру, снизу). Блок-заглушка
      «Адміністрування» с текстом и кнопкой убран совсем — кнопка администрирования уже
      есть в хедере, дублировать её в настройках владелец счёл лишним (`settings.adminTitle`
      остался только как заголовок страницы-заглушки `/admin`, `08-ui-map.md`). В «Профіль»
      одна карточка на всю левую колонку, поэтому её высота донастроена (`.logoutRow`,
      `SettingsPage.module.css`) под сумму двух карточек справа — точного совпадения при
      другом наборе полей (ошибка формы и т.п.) не гарантирует, это подгонка под типичный
      случай, не отдельный grid-механизм. Стан за замовчуванням, тема,
      вигляд каталогу/колекції и вторинна валюта — теперь редактируемые поля `user_settings`
      (обобщённый `PATCH /bootstrap/settings`, миграции 0014–0016), а не read-only заглушки
      и не только localStorage: тема и вигляд синкаются между устройствами, гривна остаётся
      базовой валютой расчётов, вторинна валюта (`USD`/`EUR`) — только выбор, что
      показывать вторым числом («≈ …»)
- [x] Місце зберігання (2026-09-13, `04-business-rules.md` §16): свой словарь
      `storage_locations` вместо свободного текста (миграция `0017`) — поле есть в форме
      покупки, в дефолте настроек и в таблице «Мої монети». Один системный пресет «Вдома»
      (изначально сеялось четыре — упрощено владельцем в тот же день: не захламлять
      выпадашку), всё остальное владелец заводит сам. CRUD своего списка живёт прямо в
      выпадашке дефолтного місця, не отдельной секцией: строка «Додати нове місце» вверху
      очищает поле под ввод, у своих вариантов — иконка видалення (пресет не трогается,
      `403`), а незнакомый typed-текст показывает подсказку «Натисніть Enter, щоб додати...»
      и сохраняется сразу по Enter или по явному выбору варианта — не только по blur. Перевод
      нового варіанта — у фоні (`BackgroundTasks` + Haiku, без ARQ); каждый ранний выход из
      этого пути залогирован (owner's report 2026-09-13: записи стабильно оставались
      непереведёнными). С логами воспроизвелось на 100% попыток и вскрыло настоящую
      причину — не сам Haiku, а порядок событий FastAPI: `BackgroundTasks` выполняются как
      часть отправки ответа, раньше коммита request-scoped сессии, поэтому
      `translate_in_background`'у открывает свою сессию и не находит только что созданную
      строку. Починено — `StorageLocationRepository.add()` коммитит сама, не дожидаясь
      общего коммита запроса.
      Заодно прибраны технические `.note`-подписи под большинством полей настроек (упоминания
      localStorage/URL-параметров юзеру не нужны) — короткие подписи остались только там,
      где реально нужно объяснить не самоочевидное поведение
- [x] `autoComplete="off"` по умолчанию на `Input`, `Textarea` и поисковые поля `Select`/
      `MultiSelect`/`Combobox` (2026-09-13, owner's report: браузер подставлял в поля вроде
      «Місце зберігання» и смены пароля в настройках не относящийся к делу текст из истории
      автозаполнения других сайтов). Экраны входа/регистрации/восстановления пароля не
      затронуты — там `autoComplete` уже проставлен явно (`email`, `current-password`,
      `new-password`) и таким полям он действительно нужен.
- [x] API под экраны: `thumbnailUrl` и `marketPriceUah` в строках коллекции,
      `GET /collection/{id}`, `GET /series/summary?countryId` (сводка по всем сериям
      одним запросом)
- [x] Автодеплой фронтенда: job `deploy-frontend` после выкладки API, rsync артефакта
      в `/srv/coinkeeper/frontend` (`10-infra.md`, «Фронтенд»)
- Отложено осознанно: миниатюры недостающих монет на дашборде, блок «Фото: офіційні з
  НБУ» и загрузка фото (этап 6), «Ввести свою ціну» (этап 5), правка личной позиции из
  интерфейса и админ-раздел (после демо), виртуализация длинных списков

**Часть 4 — визуальный рефреш ✅** (2026-09-07)

Только внешний вид: ни одного изменения в бэкенде, роутах, содержимом экранов,
API-контрактах и бизнес-логике. Бриф принят владельцем 2026-09-07, работа шла девятью
шагами, по коммиту на шаг.

- [x] Токены переписаны: светлая тема — «архивный камень» (нейтральный тёплый минеральный
      фон, слоновая кость для приподнятых карточек, латунь только как акцент), тёмная —
      уголь и тёплый графит с приглушённым старым золотом без свечения. Между фоном,
      панелью, карточкой, утопленной поверхностью и контролом теперь видимая лестница
- [x] Новые семантические токены: поверхность контрола и ховера, тонкий разделитель,
      подсветка верхней грани, фокусное кольцо, ховер строки таблицы, выбранное состояние,
      верх/низ/рамка primary-кнопки, внутренняя тень, цвета сетки, осей и серий графиков,
      токены движения (`--duration-fast/normal`, `--ease-standard`)
- [x] Playfair Display больше не назначается заголовкам по умолчанию: он остался у
      контентных заголовков (название страницы, монеты, серии, экранов входа, отдельные
      денежные значения), интерфейсные заголовки — Source Sans 3
- [x] Примитивы `shared/ui` перестроены на токенах; у `Card` появился `variant="panel"`
      для структурных панелей (фильтры, графики, таблицы, колонки обзора)
- [x] Текстовые глифы (`×`, `↺`, `☰`, `↑`, `↓`, `✕`, `✓`, `←`, `→`, `⇅`, `⤢`, `☾`, `☀`)
      заменены иконками `lucide-react`; доступные подписи не менялись
- [x] Вторая серия графика «Витрати за місяцями» — отдельный семантический токен
      (приглушённая олива) вместо производной от золота, через `useChartPalette`
- [x] `prefers-reduced-motion`: движение выключается глобально, кроме спиннера
      (`.motion-essential`); ховер-подъёмы гасятся точечно в четырёх местах

**Заглушки, оставшиеся в интерфейсе:** `/admin`
(архивация, этап 5+), кнопка «Ввести свою ціну» в карточке (этап 5), загрузка фото
(этап 6), «карта повноти» в каталоге.

**После MVP — вернуть импорт с uCoin.** 2026-09-16 обещание функции полностью убрано
из интерфейса вместе с маршрутом-заглушкой `/import`. Для быстрого восстановления прежние
точки входа: ghost-кнопка в шапке непустой страницы «Мої монети»; тихая ссылка под двумя
CTA в empty state экранов «Огляд», «Мої монети», «Серії» и «Гроші». Все пять ссылок вели
на `/import`, где был `ComingSoon`; текст был ключом `catalog.importUcoin` в UA/EN.
Общий `note`-проп `EmptyState` и его CSS также удалены как больше нигде не используемые.
- Мобильная вёрстка каждого экрана: одноколоночный вариант проектируется первым
- **Локализация: английский и украинский.** Английские строки берём из
  `legacy/ui-strings.json`; **украинские пишем заново** — в бандле их нет, там русские и
  английские (`08-ui-map.md`). Это отдельная задача этапа, а не побочный эффект вёрстки

**Часть 5 — прокрутка и тултипы графиков ✅** (2026-09-09)

Только внешний вид и поведение, без изменений в бэкенде и содержимом экранов.

- [x] Скролбар один на всё приложение (`tokens.css`): 8px, скруглённый ползунок, без
      стрелок; локальные копии стилей в дашборде и `Select` убраны. Стандартные свойства
      под `@supports not selector(::-webkit-scrollbar)` — иначе Blink глушит ими
      псевдоэлементы и рисует платформенный `thin`
- [x] Прокрутка переехала из окна в область под шапкой (`AppLayout` → `.scrollArea`):
      место под скролбар зарезервировано всегда, поэтому содержимое не прыгает, но сам
      жёлоб лежит на грунте и, пока пуст, не виден. Шапка вне контейнера прокрутки и
      всегда идёт до края окна; на телефоне прокрутка остаётся у документа
- [x] «Наверх» и блокировка фона под модалкой — через `shared/lib/pageScroll.ts`,
      он находит живой контейнер сам (`ScrollToTop`, `Pagination`, `Modal`, `Lightbox`)
- [x] Тултипы графиков расходов приклеены к курсору и подняты над легендой
      (`08-ui-map.md`, «Гроші»)

**Часть 6 — материал в списках каталога ✅** (2026-09-09)

Просьба владельца: визуально одинаковые монеты сильно расходятся в цене, и по плитке
непонятно почему. Данные уже были в `CatalogListItem` (`composition` / `material`),
показывали их только на карточке монеты.

- [x] Общий хелпер `shared/lib/coinMaterial.ts`: название из справочника, затем
      свободный текст, иначе ничего. Металл (`metalKind`) в списках не подставляется —
      это фасет фильтра, а не материал; фолбек на него остался только на карточке
      монеты, где строка подписана
- [x] Плитка: материал справа от номинала, укорочен до двух слов (`shortMaterial`),
      хвостовое короткое слово отбрасывается — «Сталь…», а не «Сталь із…»
- [x] Таблица: новая колонка «Матеріал» после номинала (без сортировки — поля для неё
      в API нет), колонка «Монета» ограничена 26 % ширины, длинное название обрезается
      в две строки, как на плитке; `min-width` таблицы 860 → 980px
- [x] Заголовки таблиц центрированы по всем колонкам — глобальным правилом `th` в
      `tokens.css`, одинаково в каталоге, «Моїх монетах» и «Грошах»; классы выравнивания
      переехали с заголовков на ячейки
- [x] Значок сортировки не участвует в центрировании: распорка в его ширину с другой
      стороны кнопки, по центру колонки стоит само название
- [x] **Все таблицы сайта — на один общий компонент** (`shared/ui/DataTable`): панель,
      шапка, ритм строк, элемент сортировки. Сортируется каждая колонка в каждой из трёх
      таблиц (каталог, «Мої монети», «Гроші»), состояние — в адресе. Бэкенд: `sort=material`
      в каталоге, восемь полей сортировки в `/collection`, `sort`/`order` в `/expenses`
      (`03-api-contract.md`)
- [x] Выравнивание одним правилом: заголовки по центру, по центру и всё содержимое,
      кроме названий и описаний (решение владельца 2026-09-09). Заодно выяснилось, что
      в «Моїх монетах» и «Грошах» суммы и раньше не прижимались вправо — класс `.number`
      проигрывал по специфичности
- [x] «Гроші»: обе безымянные колонки получили заголовки («Дії», «У доларах» —
      зарезервированная под курс НБУ, `BACKLOG.md`), пустая ячейка действий больше
      не показывает прочерк
- [x] Таблица «Мої монети» — то же лечение: свои ширины у колонок, серия в две строки,
      название в фиксированном двухстрочном окне (под ним есть вторая строка, поэтому
      просто обрезки мало), плашка «В архіві» — на эту же вторую строку. Ширины и высота
      строк проверены на трёх выборках: одинаковые
- [x] Таблица перестала дышать: явные ширины колонок, серия и материал в две строки,
      общая ширина у обеих форм действия в строке — высота строк одна на всю таблицу,
      колонки не сдвигаются между страницами
- [x] Значки сортировки одинаковые во всех состояниях (`SortIcon`): двойной шеврон,
      у активной колонки подсвечена половина — вместо одиночной стрелки, выбивавшейся
      из шапки
- [x] Миграция `0007`: 24 записи легаси-базы держали в `material` технический токен
      (`nickel_silver`, `silver`, `gold`) — код разрешён в `composition_id`, голый металл
      переписан формулировкой НБУ («срібло», «золото»). `parse_material` узнаёт код,
      `plain_material` — голый металл, повторный легаси-импорт их не вернёт
      (`02-data-model.md`). Словаря материалов на фронте нет: названия приходят с бэкенда

**Часть 7 — единая страница «Додати» ✅** (2026-09-14)

Решение владельца, отменяющее решение 2026-09-07 «ручного создания личной позиции в
интерфейсе нет». Покупка жила на одном роуте, расход — в модалке на «Грошах», а монету,
которой нет в каталоге, записать было нечем: импорт с uCoin — этап 6, и до него человек
упирался в тупик.

- [x] Одна страница `/collection/add` с полем «Тип» первым: «Покупка монети» плюс все
      ручные категории расходов; тип в адресе (`?type=`), общие поля (сумма, валюта,
      дата, продавець, нотатка) переживают переключение
- [x] Ветка покупки: країна (все ~260 эмитентов, поиск в выпадашке) → назва с живыми
      подсказками внутри страны; саджест выбран — прежний вид формы покупки без изменений
      (общий компонент `SelectedCoin` на обе страницы), саджеста нет — блок «Про монету»
      (рік, тип и матеріал обязательны, остальное под «Більше деталей»).
      `CatalogItemPicker` как отдельный шаг упразднён
- [x] Составной `POST /collection`: `catalogItemId` **или** вложенный `newCatalogItem`,
      ровно одно из двух. Позиция, экземпляр и расход `coin_purchase` — одной
      транзакцией; курс, валюта и место хранения разрешаются до первой записи, а коммит
      идёт явно внутри запроса, потому что `BackgroundTasks` у FastAPI выполняются раньше
      закрывающего коммита сессии (`04-business-rules.md`, пп. 4 и 16)
- [x] `GET /catalog/lookup` — поиск для подсказок: те же правила видимости слоёв, но без
      витринного правила вообще (ни `is_active`, ни `catalog_confirmed`), иначе форма
      предлагает США и не находит по ним ничего (`04-business-rules.md`, пп. 13, 13a)
- [x] `GET /materials`, `GET /edge-types`, `GET /quality-types` — полные словари для формы;
      материал в одном поле со свободным вводом, гурт и якість — только словарные
- [x] Фоновый перевод названия поверх существующего `translate_short_phrase`: свой промпт
      под нумизматику, слот языка оригинала — посимвольная копия введённого, при любой
      ошибке остаётся оригинал; `official` и `manual` не перетираются
- [x] Ветка расходов: та же форма плюс необязательная «Пов'язана монета» — только выбор
      существующей позиции, `catalogItemId` в запросе; привязка видна в журнале «Гроші»
      (`coinTitle` теперь приходит для любой траты с монетой). «+ Додати витрату» ведёт
      на страницу, правка расхода осталась модалкой
- [x] `amount` в `POST/PATCH /expenses` приведён к `> 0` (фронт этого требовал всегда)
- [x] Все входы «+ Додати покупку» переведены на новый роут; `/collection/coins/new` и
      `/collection/new` — редиректы с сохранением query-строки
- [x] Правка по скриншоту владельца (2026-09-14): рік випуску — выпадашка со свободным
      вводом, как «Рік від/до» в фильтрах; номінал и серія — то же самое вместо неактивных
      полей для страны без справочника. Миграция `0018` даёт им текстовые колонки-фолбэки
      (`denomination_text`, `series_text`) по образцу `composition_id`/`material`;
      `series_text` — только для показа, комплектность считается по общим сериям
- [x] Вторая правка по скриншоту (2026-09-14): вводный текст блока — про личную коллекцию,
      а не про каталог, и без перечня обязательных полей; три поля каталожных номеров
      схлопнуты в одно «Каталожний номер» (миграция `0019`, колонка `catalog_number`);
      «Нотатка про монету» заменена тремя описаниями — Опис, Опис аверса, Опис реверса —
      которые ложатся в `descriptions` в форме, зафиксированной `02-data-model.md`
- [x] «Ціна за шт.» по умолчанию `0` с выделением при фокусе: монету нашли в сдаче или
      получили от друзей — покупка записывается одним нажатием, а не через ошибку
      валидации. В `CLAUDE.md` записано, что на файлы из `docs/current_ref/` нельзя
      ссылаться ниоткуда — это служебная папка для скриншотов, а не документация
- [x] Иконки правки и удаления в каждой строке журнала «Гроші», как на «Моїх екземплярах»:
      у покупки они ведут к экземпляру, у ручного расхода — к самому расходу
- [x] В «Пов'язаній монеті» убран выбор страны (поиск по всем эмитентам), а выбранная
      монета дублируется строкой с кнопкой «Обрати іншу монету» внутри самого блока
- [x] Обратная сторона: свёрнутый блок «Пов'язані витрати» внизу формы покупки — доставка,
      холдер, грейдинг одной транзакцией с монетой (`extraExpenses` в `POST /collection`,
      курсы всех валют разрешаются до первой записи). Связь только через `catalog_item_id`:
      удаление такой траты монету не трогает, удаление монеты трату не стирает

**Часть 8 — аватарка профиля ✅** (2026-09-14)

В шапке и меню аккаунта на месте аватарки был силуэт, и поменять его было нечем.

- [x] Одна колонка `users.avatar_key` (миграция `0020`), а не запись в `media_files`: у той
      таблицы CHECK привязывает файл к каталожной записи или экземпляру, а `source`/`role`
      отвечают на вопросы, которых у портрета нет (`06-media-storage.md`)
- [x] Один размер 256 px WebP вместо трёх: аватарка показывается в 26–40 css px, 256
      покрывает 3x-экран. Ключ `users/{id}/avatar/{sha256[:12]}_256.webp` — хеш исходника
      в имени меняет URL при замене, и кеш не отдаёт прежнее лицо
- [x] `PUT` и `DELETE /auth/me/avatar` принимают и отдают сырые байты без multipart:
      `python-multipart` в зависимостях нет, и один файл без полей в конверте не нуждается.
      `PUT` идемпотентен, `DELETE` отвечает `200` с профилем — фронту нужен пустой `avatarUrl`
- [x] `avatarUrl` (подписанная ссылка на час, не ключ) собирается в единственной точке —
      `services/avatars.user_out`, поэтому приходит одинаково из `/auth/me`, `/auth/login`,
      `/auth/verify-email` и `/bootstrap`
- [x] Кадрирует браузер (`react-easy-crop`: круглая маска, drag, zoom), сервер всё равно
      ре-валидирует и центр-кропит — эндпоинт открыт любому клиенту, не только нашей форме.
      Удаление фона не применяется: оно настроено на белый фон снимка монеты
- [x] Pillow уходит в рабочий поток (`anyio.to_thread`), тело сверх лимита отбивается по
      заголовку `Content-Length` до чтения, а Caddy режет запрос ещё раньше
      (`request_body { max_size 13MB }` на `/api/*`, `10-infra.md`; строчка добавлена в
      блок сайта центрального Caddy руками — автовыкладкой она туда не попадает)
- [x] Правка по скриншоту владельца (2026-09-14): в карточке «Профіль» было две кнопки,
      и первая («Зберегти») сохраняла ровно одно поле — ім'я, хотя выглядела как кнопка
      всей карточки. Ім'я было единственным контролом на странице с явным сохранением;
      теперь оно коммитится по Enter и по уходу фокуса, как «Місце зберігання» рядом, а
      неизменённое значение запроса не шлёт. Кнопка у пароля остаётся: смена пароля —
      осознанное действие из трёх полей, отзывающее все сессии
- [x] Кнопка «Вийти» убрана из настроек: она дублировала пункт меню аккаунта, а экран
      настроек должен менять настройки, а не завершать сессию
- [x] Колонки настроек снова сходятся внизу, но уже без ручной подгонки: `align-items:
      stretch` плюс `flex-grow` на последней карточке каждой колонки — слабину добирает
      та сторона, что короче. Прежний `margin-top: 82px` был подогнан под высоту
      2026-09-13 и поехал ровно в тот момент, когда над ним появилась аватарка. На узком
      экране растягивание выключено: там колонка одна, и тянуть карточку не подо что

**Часть 9 — фото своих монет ✅** (2026-09-14)

Продолжение части 8: тот же raw-bytes `PUT`/`DELETE`, тот же перенос Pillow в рабочий
поток, тот же кроп-диалог — но теперь для фотографий экземпляров коллекции, которые (в
отличие от аватарки) обязаны жить в `media_files` и никогда не задевать общий каталог.

- [x] `PUT`/`DELETE /collection/{itemId}/photos/{role}` (`role: obverse|reverse`): всегда
      новая строка `media_files` с `collection_item_id` (не `catalog_item_id`), `owner_id`,
      `source = user_upload`; ключ — только через `media_keys.collection_base`
      (`users/{owner}/{item}/{role}/...`), пересечься с `catalog/...` не может по
      построению. `app/services/collection_photos.py` — по образцу `avatars.py`: новый
      объект и новая строка пишутся раньше, чем удаляются старые, порядок обратного
      случая не отличается от `AvatarService`
- [x] Тест-инвариант (`tests/test_collection_photos.py`): «посаженная» каталожная строка
      `source=nbu` и её ключ в бакете переживают загрузку, замену и удаление юзерского
      фото без единого изменения — это единственная проверка, которая ловит будущий
      рефакторинг, если тот случайно тронет чужую строку, а не просто полагается на
      архитектуру. Плюс изоляция между пользователями и `404` на чужой `itemId`
- [x] Выбор фото на чтение — не новый код, а то, что уже стояло в `media_urls.py`:
      `_SOURCE_PRIORITY` ставит `user_upload` первым, `MediaRepository` прячет его от
      всех, кроме владельца. Добавлен только источник данных для «фото своих экземпляров
      каталожной позиции» — `MediaRepository.owned_instance_media_for_catalog_items`
      (джойн через `collection_items` владельца) и общий хелпер
      `media_urls.images_by_catalog_item`, которым теперь пользуются и каталог, и список
      коллекции; для карточки одной покупки (`GET /collection/{id}`) — отдельный путь
      `CollectionService._instance_images`, который берёт фото именно этого экземпляра,
      а не любого из покупок той же монеты
- [x] `CollectionItemOut` получил `obverseImage`/`reverseImage` (эта покупка, не позиция
      в среднем) и `obversePhotoIsOwn`/`reversePhotoIsOwn` — фронту нужно отличать «это
      моё фото, можно удалить» от «это фото каталога», а по одному URL это не определить
- [x] Фронт: `cropAvatar.ts` разошёлся на общий `shared/lib/cropImage.ts`
      (`cropToSquareBlob`) и `AvatarCropDialog`/`CoinPhotoCropDialog` — тонкие обёртки
      над общим `shared/ui/CropDialog`. `SelectedCoin` (общий для «Додати» и правки
      покупки) получил необязательные `photos`/`onPickPhoto`/`onRemovePhoto` — карандаш
      по ховеру, кнопка в углу на тач-устройствах; без них компонент остаётся прежней
      витриной. Два таких же плейсхолдера — в «Про монету» для монеты «с нуля»
- [x] «Додати»: фото копятся как `Blob` в состоянии формы, заливаются `PUT`-ами
      последовательно **после** успешного `POST /collection` — черновиков на сервере нет.
      Упавшая заливка не откатывает уже сохранённую покупку: тост
      `purchase.photoUploadFailed`, а исправить можно со страницы правки
- [x] Правка покупки: сохранение в кроп-диалоге сразу шлёт `PUT`, ответ (свежие
      `CoinImageOut` на обе стороны) идёт в локальный стейт без рефетча всей монеты;
      кнопка удаления — только на стороне с `…PhotoIsOwn`
- [x] Редактор фото монеты доработан (2026-09-14): зум до 5× (у аватарки остался 3× —
      диапазон вынесен в пропсы `CropDialog`, не в глобальную константу), наклон ±15°
      (`react-easy-crop`, `rotation`), и настоящая круглая alpha-обрезка вместо визуальной
      маски над квадратом — `frontend/src/shared/lib/rotatedCircleCrop.ts`, WebP с
      прозрачностью и PNG-fallback (никогда не JPEG). Плюс мягкая клиентская эвристика
      размытости (вариация Лапласиана, `features/collection/blurCheck.ts`) — предупреждает,
      не блокирует. Аватарка не тронута: `cropToSquareBlob`/`AvatarCropDialog` работают как
      прежде. Бэкенд не менялся — `process_image` уже пропускал вырезание фона для
      RGBA-источника и Pillow уже сохранял альфу через все WebP-варианты, закреплено тестом
      (`06-media-storage.md`, «Кадрирование фото монеты в браузере»)

Сознательно не в этой части: суточная квота 20 МБ и лимит «4 изображения на экземпляр»
из `06-media-storage.md` — при двух ролях (аверс/реверс) и замене строки на каждой
загрузке лимит выполняется сам по себе, отдельный счётчик не потребовался.

**Часть 10 — футер, донат, саппорт ✅** (найдено готовым при ревизии 2026-09-23,
дата коммита не восстановлена; решение о включении в MVP — 2026-09-15,
`01-scope-mvp.md`)

- [x] Футер сайта — `frontend/src/app/layout/SiteFooter.tsx`, подключён в
      `AppLayout.tsx`
- [x] Кнопка доната — ссылка на монобанку (`shared/config/donation.ts`,
      `DonationDialog.tsx`)
- [x] Телеграм-чат саппорта: гостевая и авторизованная ссылка
      (`useSupportLink.ts`, `/support/telegram` и `/support/telegram/link`)
- [x] Ссылки на донат и саппорт-чат в футере
- Тесты: `SiteFooter.test.tsx`, `DonationDialog.test.tsx`

**Критерий приёмки:** новый пользователь регистрируется с телефона, подтверждает адрес,
находит монету, добавляет покупку, заводит личную позицию для того, чего нет в каталоге,
видит корректные суммы на дашборде; интерфейс полностью переведён на украинский и
английский, непереведённых строк нет.

---

## Этап 4.5. Украинский конвейер ✅

Между фронтендом и этапом 5, по решению владельца: демо имеет смысл показывать с
настоящими фотографиями и украинскими названиями, а не с плейсхолдерами.

**Закрыт целиком решением владельца 2026-09-15**: разложенный остаток (наборные монеты
НБУ, записи вне е-каталога, позиции «по физическому носителю», дыры фото) разобран,
финальная проверка полноты и дублей по украинской части каталога выполнена
(`docs/HANDOFF.md`, §3). Пункт «Часть C, остальной охват» ниже — про СССР/США, вне
периметра украинского этапа, закрытию не мешает; отдельная задача, когда до неё дойдёт
очередь (`docs/BACKLOG.md`).

- [x] **Часть A — разведка источников и сверка полноты (2026-09-03).**
  `backend/scripts/recon_ukraine.py` + `app/ukraine_recon/`: индексы НБУ / ua-coins /
  Википедии в едином формате, наш каталог read-only, таблицы «год × источник» и
  «серия × источник», сопоставление по стратегиям A/B/C/C1, кандидаты на добавление,
  фото, цены, названия, права. Итоги и исправленные предположения —
  `05-integrations.md`, раздел 8; runbook — `backend/README.md`. Главные находки:
  каталог НБУ отдаётся одним POST по 100 карточек со всеми полями и фото 1600 px;
  ua-coins.info не отвечает из-за пределов Украины (разведка идёт по копии Wayback,
  доступность с Hetzner — первый вопрос боевого прогона); русская локаль ua-coins даёт
  ключ к нашим русским названиям; по годам 1995–2018 три источника сходятся один в один.
  Боевой прогон против базы — за владельцем.
- [x] **Часть B — модель и конвейер (2026-09-04).** Две половины.

  **Трёхъязычная модель каталога** (миграция `0003`, `02-data-model.md`): у каждой
  именованной сущности — страны, серии, монеты — `*_original` на языке эмитента плюс
  `original_lang`, и два перевода со своим `*_source`. Русский перестал быть языком:
  колонки `title_ru` / `name_ru` / `label_ru` удалены, для СССР русский — это оригинал.
  Заодно перестало быть прозой то, что прозой быть не должно: страны засеяны все 260
  эмитентов (ISO 3166-1 из CLDR + исторические), номинал разобран в `value` + `unit` +
  `currency_code` с подписью по локали, материал — в справочник `materials` плюс масса и
  диаметр. Ответы API идут в локали запроса (`?locale=` / `Accept-Language`).

  **Конвейер** (`scripts/ukraine_pipeline.py`, `05-integrations.md`, раздел 9): шаги
  `bridge → series → gaps → repair-gaps → merge → titles → photos → prices`, каждый
  идемпотентен и ничего не пишет без `--apply`. `bridge` сопоставляет наши записи с
  монетами через словарь ru↔uk и сворачивание букв; спорное уходит в CSV на ревью
  человеку. Фото — один раз в свой MinIO, в трёх размерах (300/600/1200, WebP q80,
  миграция `0004`), первоисточник НБУ, где у него полноразмерного фото нет —
  ua-coins 600 px; хотлинки uCoin у украинских записей вычищены. Репетиция на копии
  боевой базы: связано 797, в ревью 160, создано 237, названий 784, цен 780. Боевой
  прогон — по runbook в `backend/README.md`, за владельцем.
- [x] **Часть B, второй прогон — четыре дефекта моста и `gaps` (2026-09-04).**
  По `bridge-review-2.csv`: 42 из 62 позиций ревью оказались записями, которые `gaps`
  же и создал. Найдено и починено:

  1. `gaps` создавал записи без номинала, вида металла и части серий — НБУ пишет
     «1 грн» и «срібло», а разборщики знали только «1 гривня» и русские названия
     сплавов. Чтение кластера в наши колонки собрано в одно место (`read_cluster`);
     для уже созданных 96 записей — шаг `repair-gaps`, дозаполняющий по `source_key`
     **только пустые** колонки.
  2. `bridge` считал ссылкой только ua-coins, поэтому запись с `source_key = 'nbu:<id>'`
     сравнивалась по названию с нашей одноимённой. Теперь ссылкой считается ключ любого
     из трёх источников, и решение из CSV применяется в том числе к позиции, у которой
     кандидатов не было вовсе.
  3. `gaps` не создаёт запись, если у нас уже есть непривязанная того же года и
     номинала: пара уходит в `--duplicates-out` пометкой `would_duplicate`. Для уже
     созданных дублей — шаг `merge`: CSV пар с описанием карточки НБУ и общими словами,
     по решению `yes` переносит экземпляры, фото и всё остальное на выжившую запись,
     старую архивирует и удаляет; количества и суммы до/после — в отчёте.
  4. Обиходные памятные 10 грн («Ми сильні. Ми разом. <область>», ССО, ТрО) в
     нумизматическом каталоге не числятся — есть только карточка ролика из 25 монет
     номиналом 250 грн. Монета читается с неё (250 ÷ 25), 25 карточек 2022–2026;
     на живых данных все они связываются со счётом 100 и получают английское название.
- [x] **Обиходные монеты — мини-конвейер, прогнан в бою (2026-09-04).** Копейки
  1/2/5/10/25/50 и гривны 1/2/5/10 — отдельная группа шагов `circ-reclassify →
  circ-bridge → circ-gaps → circ-titles → circ-mintage → circ-photos`,
  переиспользующая `runner`/`report`/`catalog` часть B (`05-integrations.md`,
  раздел 10). Мост здесь без нечёткого сравнения: ключ «номинал + год» из
  таблицы Википедии «Тиражі та хронологія…» однозначен сам по себе. Карта типов
  (`app/ukraine_pipeline/circ_types.py`) читана с «Про монети»
  bank.gov.ua/ua/uah/obig-coin, а не придумана: даты введения в обращение, металл,
  масса, диаметр и фото — оттуда, включая разделение образца 1992/1995–2018 и
  образца 2018 для гривны без пересечения годов, а с 2026-09-04 — ещё и
  разделение самой 1 гривны на образец 1992 (1992–2003) и образец 2004
  «Володимир Великий» (2004–2017), у которых до этого было одно общее и
  ошибочное фото. Пробный прогон против пустой базы и живых источников: 168
  создано, 1 обоснованно пропущена, 336 фото, 0 ошибок. **Боевой прогон против
  базы владельца, 2026-09-04: 191 запись** с названием, тиражом и фото; ручные
  правки (ревью моста, шесть юбилейных 1 грн переведены вручную) и остаточные
  хвосты (тройка 2018 года без `subtype`, девять без ячейки Википедии, 21
  строка ревью моста, довогон `circ-photos --circ-refresh-types
  hryvnia_1_2004` после разбиения 1 гривны) — `docs/BACKLOG.md`.
- [x] **Фикс: `circulation` была загрязнена памятными NBU-связанными монетами
  (2026-09-04).** Легаси-эвристика `groupFor` (`04-business-rules.md`, п.11)
  отправляла в `circulation` юбилейные обиходные 1 грн (2004–2016) и
  карбованцевые памятные 1995–1996, уже связанные с нумизматическим каталогом
  НБУ. Новый первый шаг `circ-reclassify → circ-bridge → circ-gaps →
  circ-titles → circ-mintage → circ-photos` переводит такие записи в
  `commemorative`; предохранитель во всех пяти остальных шагах игнорирует
  NBU-связанные записи независимо. Прогнан в бою вместе с прогоном выше;
  шесть записей без NBU-связи вообще (`is_nbu_linked` не видит) переведены
  вручную SQL-ом. Детали — `docs/BACKLOG.md`, `05-integrations.md` раздел 10.
- [x] **Варианты, тройка 2018 года, юбилейные, инвентаризация части B —
  код готов, боевой прогон за владельцем (2026-09-05).** Новый шаг
  `circ-variants` закрывает 21 нерешённую строку `circ-bridge-review.csv`
  через `catalog_variants` (модель была готова с `0001`, миграция не
  понадобилась) — подпись варианта только из уже сохранённых данных
  uCoin-импорта, не угадывается; заодно проставляет `subtype` тройке
  2018 года по массе/диаметру. Два точечных фикса существующей логики:
  `circ_types.type_for()` теперь учитывает `subtype` (иначе 2018-датированная
  запись старого образца получила бы фото нового), `circ_mintage`
  разрешает `SUBTYPE_2004` так же, как `SUBTYPE_1992`. Новый шаг
  `jubilee-bridge` — точечный поиск НБУ для шести юбилейных 1 грн
  (2004–2016), только в ревью, без автосвязывания; живая разведка
  2026-09-05 не нашла карточки номиналом 1 гривня ни для одной темы —
  задача для человека, не тупик. `hryvnia_1_2004` получил механизм фолбэка
  на фото ua-coins.info (`circ_types.CoinType.ua_coins_id`), но реальный id
  не найден живьём. Новый шаг `inventory-b` — CSV-инвентаризация остатка
  части B с предложением `link`/`archive`/`manual` (набор без кандидата →
  `archive` с причиной, не новый enum `collection_group`). Все шаги и тесты
  — `05-integrations.md` раздел 10, `backend/README.md`; остаточные пункты
  боевого прогона — `docs/BACKLOG.md`.
- [x] **`merge-b` — слияние остатка легаси-Excel с уже связанными записями,
  код готов, боевой прогон за владельцем (2026-09-05).** Диагностика того же
  остатка части B нашла 136 несвязанных с НБУ записей вместо прежней оценки
  ~103, из которых 118 — дубли легаси-Excel-импорта, а не пробелы: пара
  «Петро Могила»/«Десятинна церква» из диагностики проходит ранжирование
  только после очистки заголовка (`clean_orphan_title`), и обе даже очищенные
  оценки ниже `bridge.REVIEW_THRESHOLD` — этим объясняется, почему часть B и
  `inventory-b` эту пару сами не нашли. Новый шаг `merge-b`
  (`app/ukraine_pipeline/merge_b.py`, сети не требует) находит пары по ключу
  (год, номинал), CSV-ревью по решению человека, apply переиспользует
  `merge.apply_merges` как есть. `05-integrations.md`, раздел 10, подраздел
  «merge-b»; `backend/README.md`, шаг 9; `docs/BACKLOG.md`.
- [x] **Часть C, украинский срез — код и тесты готовы, боевой прогон за
  владельцем (2026-09-05).** `--steps translate-c`
  (`app/ukraine_pipeline/translate_c.py`): батчи по ~20 записей в
  `claude-haiku-4-5`, `*_source = 'llm'`, идемпотентная выборка, русский
  `title_original` заменяется сгенерированным `title_uk` (сама строка — только
  в CSV-отчёте `--translate-out`); официальные и ручные названия не трогает.
  Тем же PATCH `/catalog/{id}` заодно и admin-правка трёх названий — теперь
  всегда `*_source = 'manual'`, пустая строка отклоняется. Подробности —
  `05-integrations.md`, раздел 11; `backend/README.md`. Нужен
  `ANTHROPIC_API_KEY` в `.env`; боевой прогон против ~80 записей и шести
  юбилейных 1 гривень (id 1330–1335) и экран admin-правки на фронте —
  `docs/BACKLOG.md`.
- [ ] Часть C, остальной охват — весь СССР и США, названия серий и стран,
  которых нет в CLDR. Тот же шаг с другим `country_id`, отдельной задачей.
- [x] **Три недостающие области «Ми сильні. Ми разом» — код готов, ручной
  завод записей и боевой прогон за владельцем (2026-09-08).** Честный итог
  разведки: `gaps` принципиально не заводит запись из карточки ролика
  (`is_roll_derived` в `app/ukraine_pipeline/gaps.py` — на карточке нет ни
  металла, ни серии), а `circ-*` этой серии вообще не касаются (другой
  конвейер, другой источник — раздел 10). Значит ни один существующий шаг не
  заведёт Запорізьку/Дніпропетровську/Кіровоградську область сам: девять
  наших записей этой серии, как и все три новые, — рука человека. Новый шаг
  `gaps` теперь показывает в отчёте `skippedRoll` — заголовок и год каждого
  пропущенного ролика, чтобы `bridge --dry-run` было видно, какие карточки
  источник уже отдал. Новый EXTRA_STEP `roll-series`
  (`app/ukraine_pipeline/roll_series.py`) закрывает второй пробел: серию
  этой серии `series`-шаг тоже не видит (карточка ролика её не называет),
  так что он копирует `series_id` с уже привязанной записи на соседей с
  `series_id IS NULL` — новая заведённая вручную область подхватывает серию
  без повторного ручного SQL. `05-integrations.md`, раздел 9;
  `backend/README.md`, раздел 9b; `docs/BACKLOG.md`.
- [x] **Классификатор монета/упаковка — код и тесты готовы, боевой прогон и
  ревью CSV за владельцем (2026-09-08).** Фото девяти «Ми сильні. Ми разом»
  оказались роликом (упаковкой), а не монетой: карточка ролика в
  нумизматичному каталозі НБУ этого не показывает, только геометрия самого
  фото. `app/ukraine_pipeline/classify_coin_photos.py` — геометрический
  классификатор (метрики круга: circularity, aspect, заполнение описанной
  окружности), встроен модулем как есть, добавлены зависимости
  `opencv-python-headless`/`numpy` (единственное место в проекте, где они
  нужны). Новый батч-скрипт `scripts/scan_coin_photo_packaging.py`
  сканирует уже сохранённые фото общего украинского каталога, пишет CSV с
  вердиктом `coin`/`packaging`/`no-photo`; для `Ми сильні. Ми разом.%`
  колонка `replacementUrl` заполняется автоматически — новый модуль
  `app/ukraine_pipeline/roll_photos.py` сопоставляет область по официальной
  украинской транслитерации с ua-coins.info «розмінні та обігові»
  (`/ua/show-regular-ua/`, все двенадцать областей проверены вживую
  2026-09-08, включая три недостающие у нас). `--apply` по ревью CSV
  скачивает чистый аверс/реверс, прогоняет через существующий срез фона
  (`process_image`, тот же guard, что у остального конвейера), заменяет
  `media_files`. Встройка классификатора в сам конвейер загрузки (не только
  батч после) — `docs/BACKLOG.md`, на будущее, для новых стран.
  `05-integrations.md` раздел 12; `backend/README.md`, раздел «Coin photo
  packaging scan».
- [x] **photo-upgrade — общий механизм замены фото, боевой прогон выполнен
  (2026-09-08, закрыто решением владельца 2026-09-15 о завершении этапа
  4.5).** Решение владельца
  после первой волны: никаких сверок по названию и предварительных
  классификаций — новый шаг конвейера `photo-upgrade`
  (`app/ukraine_pipeline/photo_upgrade.py`) снимает с известной страницы
  ua-coins.info **все** фото галереи (`ua_coins.parse_coin_gallery`, новый
  парсер), ранжирует классификатором (раздел 12) в три яруса (метаданные →
  геометрия → фолбэк «оставить как есть») и заменяет только при уверенном
  выигрыше по счёту — хуже, чем было, стать не может по построению; узкое
  `roll_photos.py` этим механизмом поглощается, отдельного кода под «Ми
  сильні» больше не требуется. Общая функция замены
  (`app/ukraine_pipeline/photo_replace.py`) вынесена из
  `scan_coin_photo_packaging.py` (теперь диагностического скрипта раздела
  12) — используют оба шага, без дублирования. `05-integrations.md`, раздел
  13; `backend/README.md`, раздел «photo-upgrade»;
  `docs/2026-09-08-owner-commands-photo-upgrade.md`.
- [x] **photo-upgrade — фикс OOM на проде, повторный боевой прогон выполнен
  (2026-09-08, закрыто решением владельца 2026-09-15 о завершении этапа
  4.5).** Первый dry-run на боевой
  машине (3.7 GiB RAM) упал молча сразу после строки каталога — шаг набирал
  весь результат в памяти, а не писал построчно. Переведён на стримовый
  обход: одна запись за раз, CSV дописывается и флашится построчно, отчёт
  собирается счётчиками и `BoundedList` (потолок 50 примеров) вместо
  растущих списков, прогресс — строка на каждую запись. Закреплено тестом
  на живых/мёртвых объектах (`weakref`), не измерением RSS. Команда прогона
  из файла владельца не изменилась. `05-integrations.md`, раздел 13;
  `docs/BACKLOG.md` — беглый обзор остальных шагов на тот же риск.

**Критерий приёмки:** под локалью `uk` страна называется «Україна», серии и названия
украинских памятных монет — официальные, от НБУ, у них фото аверса и реверса из своего
хранилища и цены ua-coins; в карточке видна строка оригинала, если показан перевод. Под
локалью `en` — английские названия НБУ и стран. Чипы стран — активные, форма личной
позиции — любой эмитент. СССР и США как были, но без `title_ru` в модели. Повторный
запуск конвейера ничего не перекачивает и ничего не дублирует; отчёт содержит список
позиций, для которых источник не нашёлся.

---

## Этап 4.6. Админка

Подробности, решения и состояние работ — `docs/13-admin.md`; здесь только состав и
критерий приёмки. Поднимается до демо по решению владельца 2026-09-10.

- [x] **Часть 1 — видимость фоновых задач (2026-09-10), код готов, выкатка за
  владельцем.** Таблица `job_runs` (миграция `0021`) и приём отчёта по `X-Job-Token`;
  чтение под ролью admin; экран `/admin` вместо заглушки — список прогонов, карточка
  прогона, подсветка зависших в `running`; админский телеграм-бот с привязкой чата
  кнопкой через одноразовый код (миграция `0022`), вебхуком и командой `/last`;
  отчёт из `coin-parser` (`ca23199` там). Сторож «прогон не пришёл» — код готов
  (2026-09-23, `backend/scripts/watchdog.py`, `13-admin.md` 2.7), сам крон на сервере
  ещё не заведён — владелец.
  Команды выкатки — `docs/2026-09-10-owner-commands.md`, `docs/2026-09-23-owner-commands.md`
- **Часть 2 — редактор общего каталога — вынесена из MVP решением владельца
  2026-09-23.** Ревизия 2026-09-23 нашла, что `PATCH /catalog/{id}`
  технически пускает admin править общие карточки, но без `edited_fields`,
  `audit_log` и отдельного экрана `/admin/catalog` (детали — «После MVP»
  ниже). Владелец: основной путь появления новых записей — ревью черновиков
  (часть 4, готова), точечная правка уже опубликованных записей общего
  каталога сейчас не нужна. Задача перенесена в раздел «После MVP» этого
  документа
- [x] **Часть 3 — пользователи, полностью готово (ревизия и закрытие 2026-09-23).**
  Список пользователей (`GET /admin/users`, `UsersSection.tsx`) и выдача/снятие
  роли admin через UI (`PATCH /admin/users/{id}/role`, с защитой от
  self-demote и от снятия последнего admin, `services/admin_users.py`) —
  готово, `promote_admin.py` по ssh больше не нужен для этого. Уведомление о
  новой регистрации в телеграм — готово: срабатывает на активацию аккаунта
  (`AuthService.verify_email` и Google-ветка мгновенной активации в
  `google_auth.py`), не на голый `POST /auth/register`, чтобы недоподтверждённая
  или ботом заполненная попытка не будила чат (`13-admin.md`, часть 3).
- [x] **Часть 4 — ревью новых монет — полностью готово** (коммит `d5b399a`,
  2026-09-22, найдено при ревизии 2026-09-23). `status='draft'` скрыт от
  публичной витрины, поиска и обычных пользователей
  (`repositories/catalog.py`, фильтр `status=='active'` только не-admin);
  `publish_draft`/`reject_draft` (draft → active; отклонение — архивация с
  причиной) с audit-логом (`services/catalog.py`); эндпоинты
  `GET/POST /admin/proposals/*` (`api/v1/admin.py`); экран очереди
  `ProposalsSection.tsx` + `ProposalEditor.tsx`, вкладка в `AdminPage.tsx`.

**Критерий приёмки:** ночной прогон цен виден в админке строкой с исходом и счётчиками
(готово) — а его отсутствие приходит тревогой в телеграм (код готов 2026-09-23, крон на
сервере — за владельцем, часть 1); новый пользователь виден в списке сразу после
регистрации, роль выдаётся кнопкой, о регистрации приходит уведомление в телеграм (готово,
часть 3); запись со `status = 'draft'` не попадает ни в витрину, ни в поиск, ни в
комплектность, публикуется одним действием, а отклонённая уходит в архив с причиной
(готово, часть 4). Правка уже опубликованных записей общего каталога из критерия
исключена — часть 2 вынесена из MVP (решение владельца 2026-09-23, см. выше и «После
MVP»).

**Остаётся до закрытия этапа: только установка второго крона для сторожа на сервере
(`docs/2026-09-23-owner-commands.md`) — весь код готов.**

---

## ◆ Контрольная точка: демо владельцу коллекции ✅

Не этап, а веха между этапами 4 и 5. Изначально планировалась сразу после части 3 этапа 4;
**по решению владельца перенесена после украинского конвейера (этап 4.5)** — показывать
имеет смысл с настоящими фотографиями. Ничего не разрабатывается — приложение показывается
живым людям, и по итогам пересматриваются приоритеты этапов 5–6.

**Закрыта: владелец пользуется приложением как основным инструментом коллекции постоянно,
а не разово (подтверждено 2026-09-23) — веха прошла свою цель на практике, а не одним
показом.** Второй пункт «что показываем» ниже (второй администратор проходит путь нового
пользователя на глазах) остался неподтверждённым отдельно — не блокирует закрытие вехи,
но стоит перепроверить перед этапом 7, если это ещё не случилось само по себе.

К этому моменту работает всё, что нужно для настоящей работы с коллекцией: каталог,
коллекция, покупки, деньги, серии, личные позиции. Не работает автоматика цен и импорт —
именно про них и нужно понять, что важнее.

**Что показываем:**

- **Владелец коллекции** входит под своим аккаунтом и видит **свою** коллекцию: 620
  экземпляров, реальные суммы, свои фотографии. Не демо-данные — его настоящая база,
  перенесённая в этапе 2.
- **Второй администратор** проходит путь нового пользователя целиком и на глазах:
  регистрация → письмо → верификация → вход → пустой дашборд. Пустое состояние — часть
  демонстрации, а не то, что прячут: именно его увидит каждый новый человек.

**Что собираем:** замечания и идеи, без немедленной реализации. Всё записывается, потом
раскладывается по этапам 5–6 и разделу «После MVP». Демо — источник приоритетов, а не
очередь задач на завтра.

**Правило до демо:** в этапах 1–4 ничего из этапов 5–6 не реализуется наперёд. Соблазн
«раз уж я здесь, добавлю кнопку обновления цены» надо гасить: смысл вехи в том, чтобы
приоритеты определялись увиденным, а не тем, что успели написать заранее. Заготовки под
будущее в схеме — можно и нужно (это уже заложено); работающая функциональность — нет.

---

## Этап 5. Курсы, цены, фоновые задачи ✅ (по составу ниже — решение владельца 2026-09-15)

Перекроен под фактическое состояние: курсы и цены готовы и работают на кроне, а не через
ARQ — ARQ-воркер сам переехал после MVP (решение владельца 2026-09-15, `docs/HANDOFF.md`,
§3), по необходимости.

**Готово:**
- ~~Воркер ARQ, инфраструктура задач и `GET /jobs/{jobId}` с отменой~~ — не понадобился;
  всё живёт на кроне, ARQ — после MVP, если станет нужен
- **Интеграция с НБУ (курсы) — готово, 2026-09-13**: модуль `collector/rates/` в отдельном
  репозитории `coin-parser`, не в `coin_keeper` (`docs/BACKLOG.md`); ежедневное обновление,
  догрузка исторических курсов диапазоном; `RateRepository` и заморозка курса на дату
  покупки в `coin_keeper` (`expenses.py`)
- **UA-Coins и центральная суточная задача цен — готово**: разбор страницы и цены позиции,
  обход общего каталога порциями, снимки с `created_by = NULL`, видимы всем
  (`04-business-rules.md`, п. 7) — тот же ночной крон `coin-parser`, не ARQ
- **Слой валидации цен** (`05-integrations.md`) — до записи в базу, на всех путях, готово
- Фильтр видимости снимков в выборках и в расчёте стоимости коллекции — готово
- История цен графиком в карточке, с отметкой своих снимков — готово (карточка монеты)

**Остаётся вне MVP** (решение владельца 2026-09-15, `01-scope-mvp.md`, «Откладываем»):
- Пользовательское обновление цен **личных** позиций: одной монеты и пачкой, с прогрессом
  и остановкой
- Ручной ввод цены (`POST /prices/manual`)

**Критерий приёмки:** суточная задача (`coin-parser`) проходит по украинской части общего
каталога и обновляет цены без участия пользователя; заведомо ломаная цена отклоняется со
статусом `rejected` и попадает в лог, а не в базу; курс НБУ на дату покупки виден в журнале
«Гроші». Пункты про ручное обновление личных цен и `POST /prices/manual` из критерия
исключены — они вне MVP.

---

## Этап 6. Каталог НБУ (обогащение)

Переименован и сокращён решением владельца 2026-09-15: импорт uCoin и экспорт в Excel
перенесены после MVP (`01-scope-mvp.md`, «Откладываем»), из этого этапа убраны. Загрузка
своих фотографий из исходного состава этапа уже готова — см. ниже.

- [x] **Задача `nbu-catalog-sync` — реализована, найдено готовым при ревизии
  2026-09-23** (не в `coin_keeper` — отдельный репозиторий `coin-parser`:
  `collector/countries/ua/catalog_sync.py`, функция `update_catalog`, тест
  `tests/test_catalog_sync.py`). Обход официального каталога нумизматической
  продукции НБУ, сопоставление по `source_key`/заголовку, обогащение через
  `COALESCE` (`load_cards.py`: `title_uk`, официальные фото, тираж,
  характеристики), новые выпуски — черновики со `status = 'draft'` на ревью
  админу (этап 4.6, часть 4 — готова). Задача **никогда не удаляет**: снятое
  с выпуска архивируется с причиной. Живёт на кроне, не через ARQ —
  `deploy/crontab` в `coin-parser`, **ежедневно** в 10:40 UTC
  (`run-update-catalog.sh` → `docker-compose.collector.yml`, сервис
  `update-catalog`), не еженедельно, как задумывалось изначально: по 25
  карточек за прогон, расширяется до полного парсинга серии при появлении
  нового `nbu:<id>`
- [x] ✅ **Загрузка своих фотографий с обработкой, `source = 'user_upload'`** — реализована:
  `PUT`/`DELETE /collection/{item_id}/photos/{role}`, кроп-диалог (поворот, зум до 5x,
  круговая обрезка). Вычистка EXIF/геометок — остаётся, см. `docs/BACKLOG.md`
- Фоновая загрузка 2796 изображений по `external_url`; скачанные uCoin-файлы наследуют
  `source = 'ucoin'` и его правило видимости (`06-media-storage.md`)

Вне MVP решением владельца 2026-09-15 (`01-scope-mvp.md`): импорт Excel-выгрузки uCoin,
импорт монеты/раздела uCoin по URL, скрейпинг uCoin через Playwright по запросу
пользователя, экспорт коллекции в Excel.

**Критерий приёмки:** задача по каталогу НБУ заполняет `title_uk` и заменяет uCoin-фото
украинских выпусков официальными, новый выпуск с сайта НБУ появляется в общем каталоге
черновиком на ревью, а исчезнувший из источника архивируется, а не удаляется; отчёт задачи
содержит список архивированного и кандидатов в дубликаты. **Основная часть критерия
выполнена** (`nbu-catalog-sync` + очередь ревью, ревизия 2026-09-23) — не проверено
отдельно только наличие отчёта с явным списком кандидатов в дубликаты.

Статус фоновой загрузки 2796 изображений по `external_url` (последний пункт этапа выше)
не перепроверялся ревизией 2026-09-23 — см. `docs/BACKLOG.md`, если возьмётесь за этот
пункт следующим.

---

## Этап 7. Продакшн-закалка

Сервер, домен, HTTPS и автоматическая выкладка работают с этапа 1 — здесь не разворачивание,
а доведение до состояния, в котором систему не страшно оставить без присмотра. Расширен
решением владельца 2026-09-15 — переезд в новую репу и разнесение dev/prod теперь часть
этого этапа, не постMVP-хвост.

- [ ] **Бэкапы — подтверждённо не сделано** (ревизия 2026-09-23): ни в `coin_keeper`,
  ни в `coin-parser` нет скрипта, крона или CI-шага бэкапа; всё, что есть, — ручные
  разовые `pg_dump` перед рискованными миграциями (`backend/README.md`,
  `docs/2026-09-10-owner-commands.md`), rclone нигде не настроен
- [ ] **Проверка восстановления — не сделано** (там же): ни restore-скрипта, ни теста
  восстановления, ни сверки по количеству записей
- [x] **PWA — манифест и иконки готовы** (ревизия 2026-09-23):
  `frontend/public/manifest.webmanifest`, `<link rel="manifest">` и apple-touch-icon
  в `index.html`, иконки 192/512/512-maskable физически есть, путь отдаётся билдом.
  Установка на домашний экран работает. **Service Worker не сделан** — офлайн-кеша нет,
  `vite-plugin-pwa` не подключён; если офлайн-режим не нужен для MVP, пункт можно
  закрывать без него
- Расписание системных задач включено, задачи отработали хотя бы раз на боевых данных
- **Финальный чек-лист безопасности — частично готов** (ревизия 2026-09-23):
  закрытые наружу порты Postgres/Redis/MinIO ✅ (публикуются только в
  `docker-compose.dev.yml`, прод `docker-compose.yml` — нет), CORS только на домен
  фронтенда ✅ (`CORS_ORIGINS` из env, не `*`, `main.py` подключает `CORSMiddleware`
  только если список задан), секреты в логах — беглая проверка чистая, глубокого
  аудита не было. **Не сделано:** автоматические обновления безопасности
  (unattended-upgrades — нигде не настроен), firewall и SSH-по-ключу — не проверялись
  из кода, это состояние сервера, не репозитория (`10-infra.md`)
- **Вычистка репозитория от русского языка** (код и так должен был быть английским по
  правилу проекта — переезд повод убрать остатки)
- **Переезд в `bakost-numismatics`**: новое имя репо, технический идентификатор
  `coinkeeper` → новый (пакеты, контейнеры, образ, compose, секреты CI); решение о
  приватности/публичности нового репозитория; чистка кода и настройка агентик-флоу
- **Разнесение dev/prod**: прод `numismatics.bakost.club`, дев — нынешний
  `coins.renat-ibragimov.com`; полное дублирование БД и медиа на оба окружения; первый
  промоушен — украинский каталог и коллекция владельца в prod (`10-infra.md`)

**Критерий приёмки:** чек-лист продакшн-закалки из `10-infra.md` пройден целиком (включая
пункты переезда и dev/prod, добавленные 2026-09-15); бэкап восстановлен и сверён; владелец
пользуется приложением с телефона как основным инструментом; **посторонний человек
регистрируется сам**, получает письмо, подтверждает адрес и заводит свою коллекцию; прод и
дев разведены на отдельные домены с продублированными данными.

---

## После MVP

Приоритет определяется по мере использования:

| Задача | Условие |
|---|---|
| Редактор общего каталога (этап 4.6, часть 2): полноценная правка уже опубликованных записей из UI, `edited_fields`, `audit_log`, экран `/admin/catalog` со всеми локалями, архивация активных карточек из интерфейса | решение владельца 2026-09-23 — основной путь появления и правки записей общего каталога — ревью черновиков (часть 4, готова); точечная правка старых записей сейчас не нужна. Базовый `PATCH /catalog/{id}` уже пускает admin на общие карточки — если понадобится править точечно, это можно делать им уже сейчас, без аудита и UI |
| «Повышение» личной позиции в общий каталог админом | когда станет видно, что за позиции заводят пользователи и какие из них стоит сделать общими |
| Слияние дубликатов общего каталога с перепривязкой экземпляров на каноническую позицию | до тех пор дубликаты закрываются архивацией с причиной `'дубликат'`; кандидатов даёт отчёт задачи НБУ |
| Продажи (`sales`) | если появится потребность |
| Цели коллекционирования | если появится потребность |
| Предложения о покупке | если появится потребность |
| Разновидности | сначала решить, нужны ли вообще |
| Numista | только с пользовательским API-ключом; закрыл бы цены США и СССР |
| Двухфакторная аутентификация | не планируется; OAuth (Google) из этой строки убран — теперь в MVP, решение владельца 2026-09-15, `07-auth.md` |
| Импорт uCoin (Excel, монета/раздел по URL) | решение владельца 2026-09-15, `01-scope-mvp.md` |
| Экспорт коллекции в Excel | решение владельца 2026-09-15, `01-scope-mvp.md` |
| Ручной ввод рыночной цены | решение владельца 2026-09-15, `01-scope-mvp.md`; центральное ежедневное обновление украинской части остаётся |
| Мобильные приложения (React Native / Expo) | после стабилизации API |

---

## Как вести работу

Одна задача — один этап или его часть. Перед началом читать документ, который её описывает:
ссылки указаны в каждом этапе. Отклонения от документации допустимы, но документ тогда
обновляется в том же изменении — расхождение между `docs/` и кодом хуже, чем отсутствие
документа.


### Проверка интеграции админки — 2026-09-15

Ветка видимости фоновых задач адаптирована к текущему main: миграции `0021/0022`,
актуальные типы API и локализации. Telegram закрыт для посторонних и бывших
администраторов; команды разрешены только в личных чатах. Сторож остаётся отдельной
задачей на cron. Порядок выкладки — `2026-09-10-owner-commands.md`.
