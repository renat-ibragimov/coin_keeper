# HANDOFF — Bakost Numismatics (техн. идентификатор: coinkeeper)

Выжимка для нового чата. Разделы 1–2 и 6 — факты на дату последней правки (даты стоят
внутри разделов); разделы 3–5 и скоуп-блок переписаны под решения владельца на
2026-09-15. Подробности — в `docs/` репозитория; этот файл — карта, не дубль.

## 1. Что это и где живёт

- **Продукт:** веб-каталог и коллекция монет. Бренд Bakost Numismatics (семья
  Bakost Club), монограмма BN. Интерфейс uk/en, дефолт uk. Владелец коллекции —
  брат Рената (Олександр), 620 экземпляров. Второй админ — Ренат.
- **Репо:** `github.com/renat-ibragimov/coin_keeper` (публичное; переезд в
  `bakost-numismatics` — **часть MVP, этап 7** — решение владельца 2026-09-15, было
  «после MVP»). Спеки в `docs/00…12`, бэклог `docs/BACKLOG.md`, правила `CLAUDE.md`.
- **Домен:** `https://coins.renat-ibragimov.com` (Cloudflare DNS only). API `/api/v1`,
  Swagger `/api/v1/docs`.
- **Сервер:** Hetzner `2.28.42.171`, Ubuntu 24.04, 2 vCPU / 3.7 ГБ + своп; центральный
  Caddy в docker (`/srv/caddy`, сеть `web`). Стек — `/home/deploy/coinkeeper`
  (postgres 16, redis, minio, api). Статика фронта — `/srv/coinkeeper/frontend`.
- **SSH:** порт **2222** (22 не отвечает — «No route to host», проверено 2026-09-14);
  `root` (общесерверное) и `deploy` (проект). Для долгих
  команд — tmux (сессии рвутся, «Broken pipe»).
- **CI:** пуш в `main` → check (тесты) → build → deploy (GHCR, тег = SHA) →
  deploy-frontend. Секреты в GitHub: `DEPLOY_*`, `SSH_KNOWN_HOSTS`, `SERVER_ENV`
  (весь серверный `.env` — **любая правка .env на сервере обязана дублироваться в
  секрет, иначе деплой её сотрёт**). Миграции применяются при старте контейнера —
  пуш с миграцией меняет боевую базу: дамп до пуша.
- **LLM:** `ANTHROPIC_API_KEY` стоит на сервере и в `SERVER_ENV`; на балансе ~$5
  (одноразовый конвейерный шаг `translate-c`, с тех пор удалён, съел центы). К модели
  ходят два коротких фоновых перевода по ходу работы пользователя — место хранения и
  название личной позиции каталога (`app/services/translation.py`).
- **Пароли/секреты:** только в менеджере паролей Рената. Почта в
  `MAIL_BACKEND=console`.

## 2. Архитектурные решения (не пересматривать без причины)

- Три слоя: общий каталог (`created_by IS NULL`, правит admin), личные позиции,
  коллекция. Архивация вместо удаления; серии общие, создаёт admin.
- **Трёхъязычная модель:** `*_original`+`original_lang` (язык эмитента; для Украины
  это uk), `*_uk`, `*_en`; провенанс `*_source: official | llm | manual`. `manual`
  ставится admin-PATCH-ем автоматически и никогда не перетирается конвейером;
  `llm` перезаписывается только самим translate-c.
- **`collection_group` — ЧЕТЫРЕ значения** (не два!): commemorative (1124),
  collector (387 — Excel-эвристика клала туда драгметаллы), circulation (192),
  other (1). Любые выборки писать с учётом всех четырёх.
- **Витринная видимость** (`docs/04-business-rules.md`, §13): запись общего
  каталога видима, если страна активна ИЛИ это личная позиция пользователя ИЛИ у
  пользователя есть её экземпляр; серия видима, если страна активна ИЛИ есть
  экземпляр в серии. Применено единым предикатом `storefront_visible()` в
  каталоге, «Не вистачає», сериях и агрегатах Огляда. Прямая карточка
  `/catalog/:id` доступна всегда. Активна только Украина (`id 2`); СССР (`id 3`)
  и США (`id 1`) выключены UPDATE-ом 2026-09-05 — данные и личные позиции этих
  стран не трогали, только витрину. Часть правила «владеемое неактивной страны
  видно в каталоге» помечена как временная — владелец ещё думает.
- **Фото: `-nobg` — признак обработанности.** Файлы со срезанным фоном живут под
  ключом с суффиксом `-nobg`; оригиналы никогда не перезаписываются (план
  отката). Суффикс виден в URL — фронт может отличать обработанные фото простой
  проверкой строки (задел для UI-логики, сейчас не используется).
- **Е-каталог НБУ моделирует НАБОРЫ, а не их монеты:** «Енеїда» ×9, «Державні
  символи» (канон: «Наш герб / Наш СТЯГ / Наш гімн» — не «прапор»!), «Козацькі
  клейноди» ×4 существуют у банка только карточками наборов (Енеїда 1476/1477,
  символы 1543 (н, 2022) и 1689 (c, 2025), клейноди 1499/1513). Дешёвых юбилейных
  1 грн и мелкого золота 1 грн в е-каталоге нет вовсе. Наши записи — поштучные
  монеты; это правильнее для коллекционера, линковать их «не во что» — норма.
- **Также у НБУ:** «монета» и «монета у сувенірній упаковці» — отдельные карточки
  с отдельными ценами (50 nbu + 42 ua_coins packaged-строк считаются монетами).
  Целевая модель у нас: упаковочная запись = `catalog_variants` к базовой, витрина
  показывает одну карточку с бейджем (см. §4, «Packaging/set-варианты», и
  `docs/BACKLOG.md`).
- **Медиа:** всё в свой MinIO (300/600/1200 WebP), `media_files.storage_key` — своё,
  `external_url` — хотлинк (фронт чужие URL не показывает). Видимость по источнику:
  `PUBLIC_SOURCES = (nbu, manual, ua_coins)`; user_upload/ucoin — только владельцу.
  Фото ua-coins публичны с 2026-09-05 (была регрессия-невидимость ~290 записей).
  Вопрос прав ua-coins (1264 файла) сознательно отложен до открытия регистрации.
- Код английский; промпты и доки русские; UI-строки через i18n.
- **API карточки монеты:** `GET /catalog/{id}/prices` (история цен) и
  `GET /catalog/{id}/collection-items` (строго скоуплен `owner_id` — только свои
  экземпляры); `fetchCatalog` принимает `pageSize` (сетка каталога — 30).
- **`series_breakdown`:** порядок задаётся SQL-ом (владеемые серии — по
  `ratio desc`, затем `missing asc`, затем пустые по размеру), `limit 12` —
  фронт полагается на этот порядок, не пересортировывает сам.

## 3. Состояние на сегодня

**Флоу-рефакторинг фронта по UX-документу завершён** (промпты 1–3 + серия
точечных):

- **Навигация**: два контекста — «Моя колекція» (подменю: Огляд, Монети, Серії,
  Не вистачає, Гроші) и «Каталог»; «Адміністрування» видит только admin (внутри
  ComingSoon); «Налаштування» в меню аккаунта. Роуты `/collection/*`, старые
  пути редиректятся с сохранением query. Мобильная панель: Огляд / Монети /
  Каталог / Ще.
- **Каталог**: строго read-only витрина; вход в личные позиции — через ручное добавление
  покупки. Импорт с uCoin и его `/import`-заглушка сняты до post-MVP (2026-09-16).
  Фильтры горизонтальной панелью, каскад
  серий по стране (сброс серии и номинала при смене страны), кастомный Select
  с поиском и полноценной клавиатурой (a11y: listbox/option,
  activedescendant), чипы активных фильтров, «Ще фільтри» распущен
  (номінал/метал завжди видимі), инфо-баннер про цены удалён, вид «Карта
  повноти» изъят до реализации, CTA «Додати до колекції» / «У моїй колекції»
  + «Додати ще екземпляр» на плитке и в таблице, whole-card/row клик
  растянутой ссылкой, `GRID_PAGE_SIZE=30` (кратно колонкам 1/2/3/5) —
  дизайн-коммиты владельца вечером 2026-09-06.
- **Огляд**: KPI кликабельны (Монети, Не вистачає, Комплектність, Поточна
  оцінка → Гроші; страны — отдельным блоком ниже), четыре равными плитками,
  подзаголовок без email; строка ближайшей серии ведёт на её страницу (в
  `seriesBreakdown` добавлено поле `id`); фикс `series_breakdown` — владеемые
  серии сортируются в SQL по завершённости и гарантированно доезжают до
  фронта (`limit 12`, 4 теста); центровка сабнава (все 5 экранов коллекции) и
  хедера; уплотнение — правая колонка «Фінанси»+«Курси» заканчивается вровень
  с «Найближчі…» (рост у «Курсів», контент кверху), «Розподіл за країнами»
  полноширинным рядом. Контекстный CTA «Переглянути відсутні» с
  предфильтром `?seriesId=` **УДАЛЁН** по решению владельца при уплотнении
  (строки серий и так ведут в серии).
- **Карточка монеты**: явное разделение каталожного и личного блоков («Цієї
  монети немає у вашій колекції» / «✓ … N екземплярів»), терминология единая
  с каталогом; вкладки Характеристики / Історія цін / Мої екземпляри —
  дизайн-коммит владельца. «Мої екземпляри» реализовано (снято с отложенных):
  `GET /catalog/{id}/collection-items`, строго скоуплен `owner_id`;
  `GET /catalog/{id}/prices` + рукописный SVG-чарт истории цен без новых
  зависимостей. «Глассовая» подпись фото (`backdrop-filter` на оверлее — не
  трогает blend-цепочку, но дорога на слабых мобилках, кандидат на замену при
  жалобах на лаг — см. `docs/BACKLOG.md`).
- **Empty states** на всех пяти экранах коллекции по `dashboard.isEmpty` (в
  т.ч. «Серії» и «Не вистачає» не показывают чужие нули пользователю без
  монет).
- **Плитка**: фото contain, половинки медиазоны квадратные, аверс внахлёст над
  реверсом (дизайн-коммиты владельца).

### Фото-пайплайн (срез фона)

Модуль `app/services/media_background.py` + скрипт
`scripts/remove_photo_backgrounds.py` (runbook — `backend/README.md`). Классика
без ML: сэмпл углов → белый или чёрный однородный фон → flood-fill (у тёмной
ветки жёсткий допуск — зеркальные пруфы) → граница ≥0.75 периметра → одна
компонента → круглость 0.6–0.87 (отсекает блистеры) → срез + растушёвка + трим
по alpha bbox с ~2% отступом. Guard: вход с осмысленной прозрачностью →
`skip:already_transparent`. Срез вшит в ингест
(`process_image(remove_background=True)`) — парсер и будущая ручная заливка
(этап 6) режут автоматически; миграционные пути передают `False`.

Итог прогонов 2026-09-06: **2465 cut** из 2791 (белые 1762 в три захода, тёмные
703), затем **откат 639** тёмных cut-ов по инциденту (см. ниже) — чистыми
обработано ~1826. Хвост: 171 тёмный-впритык-к-краю (дожимается снижением
порога границы тёмной ветки), 102 неоднородных, 45 fragments, 8 odd_shape, 2
белых краевых.

**Инцидент 2026-09-06**: классификатор игнорировал существующий альфа-канал;
тёмная ветка прочитала чёрную матту под прозрачностью НБУ-исходников как фон и
назначила новую альфу, обнажив скрытый мусор. Закрыт: guard
`already_transparent` + режим `--revert-transparent-originals` (639 строк
откачено, 0 сбоев). Урок: перед новым классом обработки медиа выписывать
инварианты входа.

**Известный шум**: `missing original: 320` во всех прогонах отката — это
легаси-США июньской миграции (схема имён без размерных вариантов,
`legacy-N.webp`). Владелец решил их не трогать («штаты соберу отдельно»);
готовый промпт с фолбэком лежит в столе (см. `docs/BACKLOG.md`).

**Этап 4.5 «добить Украину» по данным ЗАКРЫТ.** Каталог: 1316 общих записей
(было 1372; 56 Excel-дублей слито merge-ом), русских названий в общем каталоге 0,
фото у ~95% (7 записей без образа вовсе, 13 `withoutNbuCard`, 10 сбоев скачивания).

Что сделано боевыми прогонами 2026-09-04/05 (все runbook-и в `backend/README.md`):
- **Обиходный конвейер** (`--steps circ-*`): reclassify (22 памятных уведены из
  circulation), bridge (167 связей; ревью 18 yes), gaps (0 — всё было), titles (191),
  mintage (тиражи; баг двух строк дворов 1992 починен, refresh-рычаг), photos
  (382 фото, 11 типов; тип 1 грн разбит 1992/2004/2018, у типа 2004 карточки НБУ
  нет — 12 записей без фото, кандидат: ua-coins id 161, механизм в circ_types готов).
- **circ-variants:** «вдавлений тризуб» ×3 привязаны и заархивированы.
- **merge-b:** 56 сирот слито в NBU-двойников (ревью-CSV с ручным suggestedTwinId —
  главный инструмент точечных решений; floor 35 / margin 8).
- **translate-c:** 80 uk + 82 en + 1 серия (haiku, батчи, провенанс llm; apply зовёт
  модель заново — CSV только для контроля качества); поверх — 5 ручных канон-правок
  SQL-ом с `manual` (Volodymyr the Great, Терезки, братерство, Архістратиг, 70 років
  Перемоги. 1945–2015).
- **Линковки по кэшу НБУ:** Пулюй→459, Гоголь→421, Амосов→597 (+ прогон
  titles/photos дотянул имена и 6 фото). Параджанов решён merge-ом ранее (3100).
- **Ручные правки в проде (лог):** UPDATE 6 (юбилейные 1330–1335 → commemorative);
  5 канон-UPDATE названий; ревью-CSV: bridge 18, variants 3, merge-b 56, links 3.

**Разложенный остаток (не «долг», а классифицированные решения):**
- *Наборные монеты НБУ* (Енеїда ×9, символы ×6, клейноди ×4): названия уже канон
  (llm), ждут задачу «set/packaging-варианты» — связка с карточками наборов.
- *Вне е-каталога* (линковать некуда, названия готовы): юбилейные 1 грн ×6
  (1330–1335), 246 «30 років незалежності» (1 грн срібло), «банкноты» ×9 (серебряные
  сувенирные банкноты НБУ — судьбу решить: отдельный тип или архив).
- *Не-НБУ:* «Українська спадщина» ×2 (523/525) — происхождение выяснить.
- *Нужен физический носитель* (у брата): близнецы-копейки ×8 (магнит!), тройка
  1 грн 2018 (1517/1519/1520 — subtype по факту, потом `circ-mintage
  --circ-refresh-mintage` снимет ambiguous:3), Академія 836/839 (по фото).
- *Мелкие дыры:* 5 записей с ua-link без фото (сбои — retry), «Пасха/Великдень» и
  «Бовдури» — admin-правка названий.

**Кэш скрейпов** — `~/coinkeeper/migration-reports/ukraine-cache/` (bank.gov.ua /
ua-coins / wikipedia): HTML внутри JSON **двойно экранирован** (`\\"`), grep-ать
питоном, не sh-регэкспами; поисковые страницы многомонетны — id брать по позиции
после `media/coins/<id>/avers`.

### Скоуп и статус MVP (решения владельца до 2026-09-15)

Актуальный список входящего/отложенного — `docs/01-scope-mvp.md`; здесь — дельта
статусов с прошлого среза (2026-09-06) и решения владельца 2026-09-15.

Ещё в силе с 2026-09-06:
- **Админка** — полноценная, не ComingSoon. Требования владелец распишет
  отдельно (ждём список — не проектировать на опережение, пока его нет).
- **Публичный каталог** (решение владельца 2026-09-07, **ещё не реализовано**) —
  каталог и карточка монеты становятся публичными, без логина. Аноним видит
  справочник БЕЗ цен (вместо цены — приглашение войти) и БЕЗ owned-слоя
  (статус владения, «Мої екземпляри», фільтр «Наявність»). Цены скрываются
  на бэкенде (`null` в выдаче), не на фронте. Всё остальное (колекція,
  гроші, серії, огляд) — за логином. Реализация — отдельной задачей после
  страницы «Гроші» (страница готова, см. «Деньги» ниже), не параллелить с
  другими бэкенд-правками. Критерий готовности MVP теперь включает «аноним
  видит публичный каталог без цен» (`01-scope-mvp.md`).

**Сделано, снято с открытых вопросов:**
- **Загрузка фото пользователем** — реализована: `PUT`/`DELETE
  /collection/{item_id}/photos/{role}`, кроп-диалог (поворот, зум до 5x,
  круговая обрезка), фото при создании личной позиции. Вычистка
  EXIF/геометок из загружаемых файлов — остаётся сделать.
- **Личные позиции: редактирование и удаление из UI** — готово,
  `PATCH`/`DELETE /catalog/{item_id}`.
- **«Монети» (мої монети)** — нелогичности, о которых шла речь 2026-09-06,
  сняты; доработок владелец не планирует, вопрос закрыт.
- **Деньги — готово целиком**: курс НБУ на дату покупки через
  `RateRepository` (`expenses.py`), догрузка исторических курсов. Вопрос
  **sourcesNote** (куда девать строку атрибуции цен, была открыта с
  уплотнения Огляда) закрыт: атрибуция остаётся минимумом на плитках и
  карточке монеты, отдельного решения не требуется (`docs/BACKLOG.md`,
  «Фронтенд — прочее»). Заодно закрыт вопрос судьбы кнопки «Експорт» каталога:
  экспорт целиком уходит после MVP, см. ниже.
- **Цены — готово**: валидация подозрительных снимков до записи в БД и
  ежедневное автообновление цен украинского каталога работают через ночной
  крон `coin-parser` (отдельный репозиторий, не ARQ). Ручной ввод рыночной
  цены остаётся вне MVP.
- **Украинский каталог полностью готов.** Разложенный остаток (наборные
  монеты НБУ, записи вне е-каталога, позиции «по физическому носителю», дыры
  фото — см. список ниже) разобран, финальная проверка полноты и дублей
  выполнена. **Этап 4.5 закрыт целиком** (`11-roadmap.md`).

**Регистрация и аутентификация — логика в коде готова, боевая доставка почты
ещё нет.** `POST /auth/register`, `/verify-email`, `/resend-verification`,
`/forgot-password`, `/reset-password`, `/change-password`, rate limit
(`app/core/rate_limit.py`), honeypot (`website`) и аварийный рубильник
`ALLOW_REGISTRATION` — всё в коде и покрыто тестами
(`backend/tests/test_auth_flow.py`, коммит `d86ca28`). Но `MAIL_BACKEND` на
сервере пока `console` — письма не уходят по-настоящему, только в лог; боевой
SMTP (`10-infra.md`) остаётся настроить до открытия сервиса посторонним.
Владелец подтвердил 2026-09-15: пока почта не настоящая, флоу считается
незавершённым. **Регистрация входит в MVP** (было ошибочно вынесено «после
MVP» в более старой редакции этого файла — поправлено).

**Новое в MVP решением владельца 2026-09-15:**
- **Вход через Google (OAuth) и линковка аккаунтов** — зарегистрировался по
  email, потом можно привязать Google-вход на тот же адрес, и наоборот.
  Раздел в `07-auth.md` — решение, детали проектирования (флоу линковки,
  конфликт email) не спроектированы, отдельная задача.
- **Футер, донат, саппорт-чат** — футер сайта; кнопка доната со ссылкой на
  монобанку; телеграм-чат саппорта (идеи, вопросы, связь с админами); ссылки
  на донат и чат в футере. Не реализовано, добавлено в `01-scope-mvp.md`.
- **Разнесение dev/prod и промоушен каталога** — целевая схема в
  `10-infra.md`: прод `numismatics.bakost.club`, дев — нынешний
  `coins.renat-ibragimov.com` (у него сейчас нет пользователей и
  официального запуска не было, по факту это дев). Полное дублирование БД и
  медиа на оба окружения; первый промоушен — украинский каталог и коллекция
  владельца. Механизм: страна собирается на деве (следующая — США), готовая
  — переносится в прод целиком (записи + фото + серии), матчинг между базами
  по `source_key`, не по `id`. Пользователи и коллекции живут только в
  проде, назад не синкаются. «Закрытие» дева для отдельных пользователей
  через Cloudflare — открытый вопрос, владелец ещё обсуждает, не
  проектировать.
- **Переезд в `bakost-numismatics` — теперь часть MVP**, не постMVP-хвост
  (см. правку в §1 выше). Вместе с ним в MVP: вычистка репозитория от
  русского языка, решение о приватности/публичности репо, чистка кода и
  настройка агентик-флоу.
- **Фоновые задачи**: ARQ — после MVP, по необходимости; всё живёт на кроне.
  В MVP остаётся (а) крон обогащения украинского каталога из парсера — новые
  выпуски и официальные данные → черновики на ревью, (б) видимость прогонов
  в админке (`13-admin.md`, часть 1).

**Убрано из MVP решением владельца 2026-09-15:**
- **Импорт uCoin** (Excel, монета по URL, раздел по URL) — после MVP.
- **Экспорт коллекции в Excel** — после MVP.

Вне MVP остаются: права ua-coins на фото (решение отложено до открытия
регистрации), ручной ввод цены, «Карта повноти», импорт uCoin, экспорт в
Excel, «Запропонувати позицію», хвост фото-пайплайна, штаты/легаси-320.

**Приёмка** — не отдельный блок задач: владелец сверяется с братом по ходу
работы, не по чек-листу. Отдельного чек-листа приёмочных проверок в доках
нет и заводить не нужно.

### Открытые решения

- Фильтры «Обсяг»/«Показати архівні» — по-прежнему невидимы-но-живы в URL, не
  решено (`docs/BACKLOG.md`, «Фронтенд — прочее»).
- «Закрытие» дева для определённых пользователей через Cloudflare — владелец
  ещё обсуждает, см. «Разнесение dev/prod» выше; не проектировать.

## 4. Ближайшая цель

1. **Боевой SMTP** (`10-infra.md`) — снять регистрацию и восстановление
   пароля с `MAIL_BACKEND=console`, иначе email-флоу не считается
   завершённым (см. §3).
2. **Google OAuth + линковка аккаунтов** — спроектировать флоу (решение
   владельца 2026-09-15, детали не расписаны), затем реализовать.
3. **Футер, донат, саппорт-чат** — новое в MVP решением 2026-09-15, ещё не
   реализовано.
4. Дизайн-доводка существующих экранов — целиком через промпты с ревью (в
   правках будет и бэк).
5. Фильтры «Обсяг»/«Показати архівні» — решить судьбу (открытый вопрос,
   см. §3).
6. Требования владельца: список по админке (часть 2–4 этапа 4.6, `13-admin.md`).
7. Packaging/set-варианты.
8. **Публичный каталог** (решение 2026-09-07, ещё не реализовано) — отдельная
   задача после страницы «Гроші», которая уже готова.
9. **dev/prod-разнесение и первый промоушен** (решение 2026-09-15,
   `10-infra.md`): домены, дублирование БД/медиа, перенос украинского
   каталога и коллекции владельца в прод.
10. Обход тремя аккаунтами → точечные правки → демо брату.

Разложенные хвосты фронта и данных, снятые с этого списка после закрытия
части, — в `docs/BACKLOG.md` (секции «Фронтенд — прочее» и «Данные и
источники»).

## 5. Следом

1. Демо владельцу (аккаунт готов; пароль сменить и передать).
2. Недельный крон обогащения каталога по НБУ (новые выпуски → черновики на
   ревью, `catalog_items.status`); ARQ-воркер — по необходимости, после MVP,
   всё остальное живёт на кроне (решение владельца 2026-09-15, см. §3).
   Суточные цены UA-Coins и курсы НБУ уже в проде на кроне `coin-parser` —
   не в этом списке, готово (см. §3). «Ввести свою ціну» остаётся вне MVP.
3. Ревизия данных SQL/CSV: серии «Інші монети» (150) и «Українська держава»
   расписать; 96 добавленных без серий; сверка 1073 карточек НБУ ↔ наши связи.
4. Переезд в `bakost-numismatics` и разнесение dev/prod переехали в
   «Ближайшая цель» (п. 9) — решением владельца 2026-09-15 это часть MVP, не
   постMVP-хвост.
5. Скелет других стран по шаблону украинского конвейера, начиная с США
   (следующая страна для dev→prod промоушена, см. §3) — как в старом плане.

## 6. Рабочий процесс и уроки (пополнено 2026-09-06)

- Ритм: промпт-файл .md → Клод-код (Sonnet для механики; Opus только для проектных
  кусков с миграциями) → пуш → ревью по репе → боевой прогон по runbook,
  **по одному пункту за раз** (правило Рената, работает отлично).
- **Разведка отдельно от кода:** смешанный промпт Sonnet читает как «сделай» и
  жжёт лимит (был инцидент на пол-ляма). RECON-задачи — plan mode
  (`--permission-mode plan`), результат — файл отчёта; протокол этапов лежит
  готовым шаблоном (спрашивает режим перед каждым этапом).
- **Фикстуры парсеров — только с живых страниц** (два бага подряд от выдуманного
  HTML: карта фото-типов и тиражи 1992). Сырой HTML — на диск, не в контекст.
- Всегда `--dry-run` перед `--apply`; дамп перед миграцией и перед большими apply;
  ревью-CSV utf-8-sig; файлы ревью — `/home/deploy/*.csv` + `-v ...:ro`.
- `docker compose run --no-deps` для разовых скриптов (без — ждёт/поднимает
  зависимости); psql с `-P pager=off`; недетерминизм LLM: то, что видел в CSV, и
  то, что записал apply, — два разных вызова, финал проверять SELECT-ом.
- Санити-чек глазами после каждого боевого прогона обязателен: три из четырёх
  реальных багов сегодня нашёл Ренат по витрине, а не отчёты.
- SQL на сервере — только в формате
  `docker compose exec postgres psql -U coinkeeper -d coinkeeper -P pager=off -c "…"`.
- Прогоны скрипта пайплайна: маунт отчётов обязателен
  (`-v /home/deploy/coinkeeper/migration-reports:/app/migration-reports`),
  длинные — из tmux, **один режим за раз**, никогда параллельно.
- scp с сервера: `scp -P 2222` (порт заглавной `P`).
- Во всех промпт-файлах — указание Клод-коду общаться в терминале по-русски
  (код/коммиты английские).
- Самостоятельные коммиты владельца по фронту: после пуша — беглый прогляд
  каталога в обеих темах (самый хрупкий экран: blend, isolation, растянутые
  ссылки); правки CoinImage/CoinCard — на ревью ассистенту. Прод смотреть
  через жёсткий F5 (Ctrl+Shift+R) — уже ловили старый бандл.
- **Blend-цепочка фото:** любые z-index / isolation / opacity / transform /
  filter на предках `<img>` режут `mix-blend-mode`. История: isolation на
  фреймах нахлёста убил растворение белого (регресс пойман на проде).
- **Ревью пушей — смотреть дифф целиком, не первые N файлов:** эндпоинты
  `/prices` и `/collection-items` однажды проскочили мимо ревью из-за
  обрезанного листинга (найдены и отревьюены постфактум, чисто).
- **Дизайн-правки владельца напрямую** — норм для чистого CSS/раскладки; всё,
  что трогает данные, запросы, API или blend-цепочку фото, — через промпт с
  ревью (договорённость вечера 2026-09-06).
