# HANDOFF — Bakost Numismatics (техн. идентификатор: coinkeeper)

Выжимка для нового чата. Всё, что здесь, — факты на 2026-09-05 (вечер). Подробности —
в `docs/` репозитория; этот файл — карта, не дубль.

## 1. Что это и где живёт

- **Продукт:** веб-каталог и коллекция монет. Бренд Bakost Numismatics (семья
  Bakost Club), монограмма BN. Интерфейс uk/en, дефолт uk. Владелец коллекции —
  брат Рената (Олександр), 620 экземпляров. Второй админ — Ренат.
- **Репо:** `github.com/renat-ibragimov/coin_keeper` (публичное; переезд в
  `bakost-numismatics` — после MVP). Спеки в `docs/00…12`, бэклог `docs/BACKLOG.md`,
  правила `CLAUDE.md`.
- **Домен:** `https://coins.renat-ibragimov.com` (Cloudflare DNS only). API `/api/v1`,
  Swagger `/api/v1/docs`.
- **Сервер:** Hetzner `2.28.42.171`, Ubuntu 24.04, 2 vCPU / 3.7 ГБ + своп; центральный
  Caddy в docker (`/srv/caddy`, сеть `web`). Стек — `/home/deploy/coinkeeper`
  (postgres 16, redis, minio, api). Статика фронта — `/srv/coinkeeper/frontend`.
- **SSH:** порты 22 и 2222; `root` (общесерверное) и `deploy` (проект). Для долгих
  команд — tmux (сессии рвутся, «Broken pipe»).
- **CI:** пуш в `main` → check (тесты) → build → deploy (GHCR, тег = SHA) →
  deploy-frontend. Секреты в GitHub: `DEPLOY_*`, `SSH_KNOWN_HOSTS`, `SERVER_ENV`
  (весь серверный `.env` — **любая правка .env на сервере обязана дублироваться в
  секрет, иначе деплой её сотрёт**). Миграции применяются при старте контейнера —
  пуш с миграцией меняет боевую базу: дамп до пуша.
- **LLM:** `ANTHROPIC_API_KEY` стоит на сервере и в `SERVER_ENV`; на балансе ~$5
  (часть C съела центы). Единственный шаг с внешним API — `translate-c`.
- **Пароли/секреты:** только в менеджере паролей Рената. Почта в
  `MAIL_BACKEND=console`.

## 2. Архитектурные решения (не пересматривать без причины)

- Три слоя: общий каталог (`created_by IS NULL`, правит admin), личные позиции,
  коллекция. Архивация вместо удаления; серии общие, создаёт admin.
- **Трёхъязычная модель:** `*_original`+`original_lang` (язык эмитента; для Украины
  это uk), `*_uk`, `*_en`; провенанс `*_source: official | llm | manual`. `manual`
  ставится admin-PATCH-ем автоматически и никогда не перетирается конвейером;
  `llm` перезаписывается только самим translate-c.
- **`collection_group` — ЧЕТЫРЕ значения** (не два!): commemorative (1124),
  collector (387 — Excel-эвристика клала туда драгметаллы), circulation (192),
  other (1). Любые выборки писать с учётом всех четырёх.
- **`countries.is_active` горит у ТРЁХ стран** (id 1, 2, 3; Украина = 2) — витрина
  чипов сейчас показывает три. Решение Рената: активной оставить только Украину
  (см. §4). Данные/личные позиции других стран не трогать — только витрину.
- **Е-каталог НБУ моделирует НАБОРЫ, а не их монеты:** «Енеїда» ×9, «Державні
  символи» (канон: «Наш герб / Наш СТЯГ / Наш гімн» — не «прапор»!), «Козацькі
  клейноди» ×4 существуют у банка только карточками наборов (Енеїда 1476/1477,
  символы 1543 (н, 2022) и 1689 (c, 2025), клейноди 1499/1513). Дешёвых юбилейных
  1 грн и мелкого золота 1 грн в е-каталоге нет вовсе. Наши записи — поштучные
  монеты; это правильнее для коллекционера, линковать их «не во что» — норма.
- **Также у НБУ:** «монета» и «монета у сувенірній упаковці» — отдельные карточки
  с отдельными ценами (50 nbu + 42 ua_coins packaged-строк считаются монетами).
  Целевая модель у нас: упаковочная запись = `catalog_variants` к базовой, витрина
  показывает одну карточку с бейджем (см. §4).
- **Медиа:** всё в свой MinIO (300/600/1200 WebP), `media_files.storage_key` — своё,
  `external_url` — хотлинк (фронт чужие URL не показывает). Видимость по источнику:
  `PUBLIC_SOURCES = (nbu, manual, ua_coins)`; user_upload/ucoin — только владельцу.
  Фото ua-coins публичны с 2026-09-05 (была регрессия-невидимость ~290 записей).
  Вопрос прав ua-coins (1264 файла) сознательно отложен до открытия регистрации.
- Код английский; промпты и доки русские; UI-строки через i18n.

## 3. Состояние на сегодня

**Этап 4.5 «добить Украину» по данным ЗАКРЫТ.** Каталог: 1316 общих записей
(было 1372; 56 Excel-дублей слито merge-ом), русских названий в общем каталоге 0,
фото у ~95% (7 записей без образа вовсе, 13 `withoutNbuCard`, 10 сбоев скачивания).

Что сделано боевыми прогонами 2026-09-04/05 (все runbook-и в `backend/README.md`):
- **Обиходный конвейер** (`--steps circ-*`): reclassify (22 памятных уведены из
  circulation), bridge (167 связей; ревью 18 yes), gaps (0 — всё было), titles (191),
  mintage (тиражи; баг двух строк дворов 1992 починен, refresh-рычаг), photos
  (382 фото, 11 типов; тип 1 грн разбит 1992/2004/2018, у типа 2004 карточки НБУ
  нет — 12 записей без фото, кандидат: ua-coins id 161, механизм в circ_types готов).
- **circ-variants:** «вдавлений тризуб» ×3 привязаны и заархивированы.
- **merge-b:** 56 сирот слито в NBU-двойников (ревью-CSV с ручным suggestedTwinId —
  главный инструмент точечных решений; floor 35 / margin 8).
- **translate-c:** 80 uk + 82 en + 1 серия (haiku, батчи, провенанс llm; apply зовёт
  модель заново — CSV только для контроля качества); поверх — 5 ручных канон-правок
  SQL-ом с `manual` (Volodymyr the Great, Терезки, братерство, Архістратиг, 70 років
  Перемоги. 1945–2015).
- **Линковки по кэшу НБУ:** Пулюй→459, Гоголь→421, Амосов→597 (+ прогон
  titles/photos дотянул имена и 6 фото). Параджанов решён merge-ом ранее (3100).
- **Ручные правки в проде (лог):** UPDATE 6 (юбилейные 1330–1335 → commemorative);
  5 канон-UPDATE названий; ревью-CSV: bridge 18, variants 3, merge-b 56, links 3.

**Разложенный остаток (не «долг», а классифицированные решения):**
- *Наборные монеты НБУ* (Енеїда ×9, символы ×6, клейноди ×4): названия уже канон
  (llm), ждут задачу «set/packaging-варианты» — связка с карточками наборов.
- *Вне е-каталога* (линковать некуда, названия готовы): юбилейные 1 грн ×6
  (1330–1335), 246 «30 років незалежності» (1 грн срібло), «банкноты» ×9 (серебряные
  сувенирные банкноты НБУ — судьбу решить: отдельный тип или архив).
- *Не-НБУ:* «Українська спадщина» ×2 (523/525) — происхождение выяснить.
- *Нужен физический носитель* (у брата): близнецы-копейки ×8 (магнит!), тройка
  1 грн 2018 (1517/1519/1520 — subtype по факту, потом `circ-mintage
  --circ-refresh-mintage` снимет ambiguous:3), Академія 836/839 (по фото).
- *Мелкие дыры:* 5 записей с ua-link без фото (сбои — retry), «Пасха/Великдень» и
  «Бовдури» — admin-правка названий.

**Кэш скрейпов** — `~/coinkeeper/migration-reports/ukraine-cache/` (bank.gov.ua /
ua-coins / wikipedia): HTML внутри JSON **двойно экранирован** (`\\"`), grep-ать
питоном, не sh-регэкспами; поисковые страницы многомонетны — id брать по позиции
после `media/coins/<id>/avers`.

## 4. Ближайшая цель — ФРОНТ (Ренат отвлекается от подкапотки)

- [ ] **Витрина стран:** активной оставить только Украину (id 2) — `is_active=false`
      для id 1 и 3 (СССР и США: проверить SQL-ом, кто есть кто, перед UPDATE);
      чипы/фильтры/серии показывают только активные; Украина первой везде
      (`sort_order=0` уже стоит). Записи и личные коллекции НЕ трогать — витринная
      видимость, не удаление.
- [ ] **Packaging/set-варианты:** шаг Клод-кода — «монета у сувенірній упаковці» →
      `catalog_variants` к базовой (пары по слоту + маркер в названии карточки);
      поштучные наборные (Енеїда/символы/клейноди) — связь с карточкой набора.
      Фронт: одна карточка с бейджем «є варіант у сувенірній упаковці», деталка с
      обеими ценами, комплектность по базовой.
- [ ] **UX-пачка по мдшке от ГПТ** (Ренат крутил UX с GPT, файл принесёт): плитка
      каталога (аверс крупно, реверс в углу; серебро на тёмном), дефолтная
      сортировка «памятные по году ↓, обиходные ниже», бейдж «фото типове» (типовое
      фото с чужим годом — копейка-2013, гривна-2003, золото-2010), «Джерело»
      прятать при пустом URL, «KM# KM#» дубль префикса, мобильные мелочи.
      Формат: список/моки → один прочёсочный промпт → ревью на домене.
- [ ] Хвосты доки/валидаций: `docs/06-media-storage.md` синхронизировать с фиксом
      видимости (не сделано тем же коммитом — нарушение правила); ридеру
      link-CSV — проверка существования itemId (сейчас FK-трейсбек); шумный
      грамматический разнобой банкнот («видана/випущена») — при разборе банкнот.

## 5. Следом

1. Демо владельцу (аккаунт готов; пароль сменить и передать).
2. Автоматика этапа 5: ARQ-воркер; суточные цены UA-Coins; курсы НБУ; недельный
   синк с НБУ (новые выпуски → позиции+фото+названия; свежую «До 30-річчя грошової
   реформи» 02.09.2026 конвейер уже поймал руками — синк будет ловить сам);
   «Ввести свою ціну».
3. Ревизия данных SQL/CSV: серии «Інші монети» (150) и «Українська держава»
   расписать; 96 добавленных без серий; сверка 1073 карточек НБУ ↔ наши связи.
4. Этапы 6–7, переезд в `bakost-numismatics`, скелет других стран по шаблону
   украинского конвейера — как в старом плане.

## 6. Рабочий процесс и уроки (пополнено 2026-09-05)

- Ритм: промпт-файл .md → Клод-код (Sonnet для механики; Opus только для проектных
  кусков с миграциями) → пуш → ревью по репе → боевой прогон по runbook,
  **по одному пункту за раз** (правило Рената, работает отлично).
- **Разведка отдельно от кода:** смешанный промпт Sonnet читает как «сделай» и
  жжёт лимит (был инцидент на пол-ляма). RECON-задачи — plan mode
  (`--permission-mode plan`), результат — файл отчёта; протокол этапов лежит
  готовым шаблоном (спрашивает режим перед каждым этапом).
- **Фикстуры парсеров — только с живых страниц** (два бага подряд от выдуманного
  HTML: карта фото-типов и тиражи 1992). Сырой HTML — на диск, не в контекст.
- Всегда `--dry-run` перед `--apply`; дамп перед миграцией и перед большими apply;
  ревью-CSV utf-8-sig; файлы ревью — `/home/deploy/*.csv` + `-v ...:ro`.
- `docker compose run --no-deps` для разовых скриптов (без — ждёт/поднимает
  зависимости); psql с `-P pager=off`; недетерминизм LLM: то, что видел в CSV, и
  то, что записал apply, — два разных вызова, финал проверять SELECT-ом.
- Санити-чек глазами после каждого боевого прогона обязателен: три из четырёх
  реальных багов сегодня нашёл Ренат по витрине, а не отчёты.